(********************************************************)
(*           AUTOGENERATED FILE - DO NOT EDIT!          *)
(********************************************************)
(* Generated by: ocaml-protoc-plugin                    *)
(* https://github.com/andersfugmann/ocaml-protoc-plugin *)
(********************************************************)
(*
  Source: plugin/grpc/protocol/llm.proto
  Syntax: proto3
  Parameters:
    debug=false
    annot=''
    opens=[]
    int64_as_int=true
    int32_as_int=true
    fixed_as_int=false
    singleton_record=false
    prefix_output_with_package=false
*)
[@@@ocaml.alert "-protobuf"] (* Disable deprecation warnings for protobuf*)
(**/**)
module Runtime' = Ocaml_protoc_plugin [@@warning "-33"]
module Imported'modules = struct
end
(**/**)
module rec Protocol : sig
  module rec ChatMessage : sig
    type t = {
      role:string;
      (**
{%html:
<p>&quot;system&quot;, &quot;user&quot;, &quot;assistant&quot;</p>
%}
      *)

      content:string;
    }
    val make: ?role:string -> ?content:string -> unit -> t
    (** Helper function to generate a message using default values *)

    val to_proto: t -> Runtime'.Writer.t
    (** Serialize the message to binary format *)

    val from_proto: Runtime'.Reader.t -> (t, [> Runtime'.Result.error]) result
    (** Deserialize from binary format *)

    val to_json: Runtime'.Json_options.t -> t -> Runtime'.Json.t
    (** Serialize to Json (compatible with Yojson.Basic.t) *)

    val from_json: Runtime'.Json.t -> (t, [> Runtime'.Result.error]) result
    (** Deserialize from Json (compatible with Yojson.Basic.t) *)

    val name: unit -> string
    (** Fully qualified protobuf name of this message *)

    (**/**)
    type make_t = ?role:string -> ?content:string -> unit -> t
    val merge: t -> t -> t
    val to_proto': Runtime'.Writer.t -> t -> unit
    val from_proto_exn: Runtime'.Reader.t -> t
    val from_json_exn: Runtime'.Json.t -> t
    (**/**)
  end

  and LLMChatRequest : sig
    type t = {
      system_prompt:string[@ocaml.alert protobuf "Marked as deprecated in the .proto file"];
      (**
{%html:
<p>Deprecated: use messages field for new callers.</p>
%}
      *)

      user_prompt:string[@ocaml.alert protobuf "Marked as deprecated in the .proto file"];
      (**
{%html:
<p>Deprecated: use messages field for new callers.</p>
%}
      *)

      model:string;
      temperature:float;
      max_tokens:int;
      provider:string;
      attachments:Attachment.t list;
      messages:ChatMessage.t list;
      (**
{%html:
<p>Multi-turn conversation history. When populated, takes precedence
over system_prompt/user_prompt which remain for single-turn callers.</p>
%}
      *)

      priority:int;
      (**
{%html:
<p>Queuing priority. Lower value = higher priority.
0 = interactive (user-initiated), 10 = background (generators, batch).</p>
%}
      *)

    }
    val make: ?system_prompt:string -> ?user_prompt:string -> ?model:string -> ?temperature:float -> ?max_tokens:int -> ?provider:string -> ?attachments:Attachment.t list -> ?messages:ChatMessage.t list -> ?priority:int -> unit -> t
    (** Helper function to generate a message using default values *)

    val to_proto: t -> Runtime'.Writer.t
    (** Serialize the message to binary format *)

    val from_proto: Runtime'.Reader.t -> (t, [> Runtime'.Result.error]) result
    (** Deserialize from binary format *)

    val to_json: Runtime'.Json_options.t -> t -> Runtime'.Json.t
    (** Serialize to Json (compatible with Yojson.Basic.t) *)

    val from_json: Runtime'.Json.t -> (t, [> Runtime'.Result.error]) result
    (** Deserialize from Json (compatible with Yojson.Basic.t) *)

    val name: unit -> string
    (** Fully qualified protobuf name of this message *)

    (**/**)
    type make_t = ?system_prompt:string -> ?user_prompt:string -> ?model:string -> ?temperature:float -> ?max_tokens:int -> ?provider:string -> ?attachments:Attachment.t list -> ?messages:ChatMessage.t list -> ?priority:int -> unit -> t
    val merge: t -> t -> t
    val to_proto': Runtime'.Writer.t -> t -> unit
    val from_proto_exn: Runtime'.Reader.t -> t
    val from_json_exn: Runtime'.Json.t -> t
    (**/**)
  end

  and Attachment : sig
    type t = {
      mime_type:string;
      data:string;
      filename:string;
    }
    val make: ?mime_type:string -> ?data:string -> ?filename:string -> unit -> t
    (** Helper function to generate a message using default values *)

    val to_proto: t -> Runtime'.Writer.t
    (** Serialize the message to binary format *)

    val from_proto: Runtime'.Reader.t -> (t, [> Runtime'.Result.error]) result
    (** Deserialize from binary format *)

    val to_json: Runtime'.Json_options.t -> t -> Runtime'.Json.t
    (** Serialize to Json (compatible with Yojson.Basic.t) *)

    val from_json: Runtime'.Json.t -> (t, [> Runtime'.Result.error]) result
    (** Deserialize from Json (compatible with Yojson.Basic.t) *)

    val name: unit -> string
    (** Fully qualified protobuf name of this message *)

    (**/**)
    type make_t = ?mime_type:string -> ?data:string -> ?filename:string -> unit -> t
    val merge: t -> t -> t
    val to_proto': Runtime'.Writer.t -> t -> unit
    val from_proto_exn: Runtime'.Reader.t -> t
    val from_json_exn: Runtime'.Json.t -> t
    (**/**)
  end

  and LLMChatResponse : sig
    type t = {
      content:string;
      model:string;
      prompt_tokens:int;
      completion_tokens:int;
      total_tokens:int;
    }
    val make: ?content:string -> ?model:string -> ?prompt_tokens:int -> ?completion_tokens:int -> ?total_tokens:int -> unit -> t
    (** Helper function to generate a message using default values *)

    val to_proto: t -> Runtime'.Writer.t
    (** Serialize the message to binary format *)

    val from_proto: Runtime'.Reader.t -> (t, [> Runtime'.Result.error]) result
    (** Deserialize from binary format *)

    val to_json: Runtime'.Json_options.t -> t -> Runtime'.Json.t
    (** Serialize to Json (compatible with Yojson.Basic.t) *)

    val from_json: Runtime'.Json.t -> (t, [> Runtime'.Result.error]) result
    (** Deserialize from Json (compatible with Yojson.Basic.t) *)

    val name: unit -> string
    (** Fully qualified protobuf name of this message *)

    (**/**)
    type make_t = ?content:string -> ?model:string -> ?prompt_tokens:int -> ?completion_tokens:int -> ?total_tokens:int -> unit -> t
    val merge: t -> t -> t
    val to_proto': Runtime'.Writer.t -> t -> unit
    val from_proto_exn: Runtime'.Reader.t -> t
    val from_json_exn: Runtime'.Json.t -> t
    (**/**)
  end


  (**
{%html:
<p>Streaming: one chunk per generated token</p>
%}
  *)
  and LLMChatChunk : sig
    type t = {
      token:string;
      (**
{%html:
<p>The generated token text</p>
%}
      *)

      done':bool;
      (**
{%html:
<p>True on final chunk</p>
%}
      *)

      model:string;
      (**
{%html:
<p>Model name (sent on first or last chunk)</p>
%}
      *)

      prompt_tokens:int;
      (**
{%html:
<p>Sent on final chunk</p>
%}
      *)

      completion_tokens:int;
      (**
{%html:
<p>Sent on final chunk</p>
%}
      *)

      total_tokens:int;
      (**
{%html:
<p>Sent on final chunk</p>
%}
      *)

      signal:TokenSignalProto.t option;
      (**
{%html:
<p>Per-token signal data (if available)</p>
%}
      *)

    }
    val make: ?token:string -> ?done':bool -> ?model:string -> ?prompt_tokens:int -> ?completion_tokens:int -> ?total_tokens:int -> ?signal:TokenSignalProto.t -> unit -> t
    (** Helper function to generate a message using default values *)

    val to_proto: t -> Runtime'.Writer.t
    (** Serialize the message to binary format *)

    val from_proto: Runtime'.Reader.t -> (t, [> Runtime'.Result.error]) result
    (** Deserialize from binary format *)

    val to_json: Runtime'.Json_options.t -> t -> Runtime'.Json.t
    (** Serialize to Json (compatible with Yojson.Basic.t) *)

    val from_json: Runtime'.Json.t -> (t, [> Runtime'.Result.error]) result
    (** Deserialize from Json (compatible with Yojson.Basic.t) *)

    val name: unit -> string
    (** Fully qualified protobuf name of this message *)

    (**/**)
    type make_t = ?token:string -> ?done':bool -> ?model:string -> ?prompt_tokens:int -> ?completion_tokens:int -> ?total_tokens:int -> ?signal:TokenSignalProto.t -> unit -> t
    val merge: t -> t -> t
    val to_proto': Runtime'.Writer.t -> t -> unit
    val from_proto_exn: Runtime'.Reader.t -> t
    val from_json_exn: Runtime'.Json.t -> t
    (**/**)
  end

  and TokenSignalProto : sig
    type t = {
      confidence:float;
      (**
{%html:
<p>P(chosen) from raw distribution</p>
%}
      *)

      entropy:float;
      (**
{%html:
<p>Shannon entropy in bits</p>
%}
      *)

      top_gap:float;
      (**
{%html:
<p>P(top1) - P(top2)</p>
%}
      *)

      top_k:TokenCandidateProto.t list;
      (**
{%html:
<p>Top-k candidates</p>
%}
      *)

      full_distribution:float list;
      (**
{%html:
<p>Full softmax distribution (vocab_size floats)</p>
%}
      *)

      sampler_stages:SamplerStageProto.t list;
      (**
{%html:
<p>Per-stage snapshots through sampler chain</p>
%}
      *)

    }
    val make: ?confidence:float -> ?entropy:float -> ?top_gap:float -> ?top_k:TokenCandidateProto.t list -> ?full_distribution:float list -> ?sampler_stages:SamplerStageProto.t list -> unit -> t
    (** Helper function to generate a message using default values *)

    val to_proto: t -> Runtime'.Writer.t
    (** Serialize the message to binary format *)

    val from_proto: Runtime'.Reader.t -> (t, [> Runtime'.Result.error]) result
    (** Deserialize from binary format *)

    val to_json: Runtime'.Json_options.t -> t -> Runtime'.Json.t
    (** Serialize to Json (compatible with Yojson.Basic.t) *)

    val from_json: Runtime'.Json.t -> (t, [> Runtime'.Result.error]) result
    (** Deserialize from Json (compatible with Yojson.Basic.t) *)

    val name: unit -> string
    (** Fully qualified protobuf name of this message *)

    (**/**)
    type make_t = ?confidence:float -> ?entropy:float -> ?top_gap:float -> ?top_k:TokenCandidateProto.t list -> ?full_distribution:float list -> ?sampler_stages:SamplerStageProto.t list -> unit -> t
    val merge: t -> t -> t
    val to_proto': Runtime'.Writer.t -> t -> unit
    val from_proto_exn: Runtime'.Reader.t -> t
    val from_json_exn: Runtime'.Json.t -> t
    (**/**)
  end

  and TokenCandidateProto : sig
    type t = {
      id:int;
      text:string;
      prob:float;
    }
    val make: ?id:int -> ?text:string -> ?prob:float -> unit -> t
    (** Helper function to generate a message using default values *)

    val to_proto: t -> Runtime'.Writer.t
    (** Serialize the message to binary format *)

    val from_proto: Runtime'.Reader.t -> (t, [> Runtime'.Result.error]) result
    (** Deserialize from binary format *)

    val to_json: Runtime'.Json_options.t -> t -> Runtime'.Json.t
    (** Serialize to Json (compatible with Yojson.Basic.t) *)

    val from_json: Runtime'.Json.t -> (t, [> Runtime'.Result.error]) result
    (** Deserialize from Json (compatible with Yojson.Basic.t) *)

    val name: unit -> string
    (** Fully qualified protobuf name of this message *)

    (**/**)
    type make_t = ?id:int -> ?text:string -> ?prob:float -> unit -> t
    val merge: t -> t -> t
    val to_proto': Runtime'.Writer.t -> t -> unit
    val from_proto_exn: Runtime'.Reader.t -> t
    val from_json_exn: Runtime'.Json.t -> t
    (**/**)
  end


  (**
{%html:
<p>Snapshot of token distribution after a sampler stage</p>
%}
  *)
  and SamplerStageProto : sig
    type t = {
      name:string;
      (**
{%html:
<p>Stage name: &quot;logits&quot;, &quot;top_k&quot;, &quot;top_p&quot;, &quot;temp&quot;, etc.</p>
%}
      *)

      active_count:int;
      (**
{%html:
<p>Tokens remaining with nonzero probability</p>
%}
      *)

      top1_prob:float;
      (**
{%html:
<p>P(top token) after this stage</p>
%}
      *)

      entropy:float;
      (**
{%html:
<p>Shannon entropy after this stage</p>
%}
      *)

      top_k:TokenCandidateProto.t list;
      (**
{%html:
<p>Top-5 candidates after this stage</p>
%}
      *)

    }
    val make: ?name:string -> ?active_count:int -> ?top1_prob:float -> ?entropy:float -> ?top_k:TokenCandidateProto.t list -> unit -> t
    (** Helper function to generate a message using default values *)

    val to_proto: t -> Runtime'.Writer.t
    (** Serialize the message to binary format *)

    val from_proto: Runtime'.Reader.t -> (t, [> Runtime'.Result.error]) result
    (** Deserialize from binary format *)

    val to_json: Runtime'.Json_options.t -> t -> Runtime'.Json.t
    (** Serialize to Json (compatible with Yojson.Basic.t) *)

    val from_json: Runtime'.Json.t -> (t, [> Runtime'.Result.error]) result
    (** Deserialize from Json (compatible with Yojson.Basic.t) *)

    val name: unit -> string
    (** Fully qualified protobuf name of this message *)

    (**/**)
    type make_t = ?name:string -> ?active_count:int -> ?top1_prob:float -> ?entropy:float -> ?top_k:TokenCandidateProto.t list -> unit -> t
    val merge: t -> t -> t
    val to_proto': Runtime'.Writer.t -> t -> unit
    val from_proto_exn: Runtime'.Reader.t -> t
    val from_json_exn: Runtime'.Json.t -> t
    (**/**)
  end

  module LLMService : sig
    module Chat : sig
      include Runtime'.Service.Rpc with type Request.t = LLMChatRequest.t and type Response.t = LLMChatResponse.t
      module Request : Runtime'.Spec.Message with type t = LLMChatRequest.t and type make_t = LLMChatRequest.make_t
      (** Module alias for the request message for this method call *)

      module Response : Runtime'.Spec.Message with type t = LLMChatResponse.t and type make_t = LLMChatResponse.make_t
      (** Module alias for the response message for this method call *)

    end

    val chat : (module Runtime'.Spec.Message with type t = LLMChatRequest.t) * (module Runtime'.Spec.Message with type t = LLMChatResponse.t)
    module StreamChat : sig
      include Runtime'.Service.Rpc with type Request.t = LLMChatRequest.t and type Response.t = LLMChatChunk.t
      module Request : Runtime'.Spec.Message with type t = LLMChatRequest.t and type make_t = LLMChatRequest.make_t
      (** Module alias for the request message for this method call *)

      module Response : Runtime'.Spec.Message with type t = LLMChatChunk.t and type make_t = LLMChatChunk.make_t
      (** Module alias for the response message for this method call *)

    end

    val streamChat : (module Runtime'.Spec.Message with type t = LLMChatRequest.t) * (module Runtime'.Spec.Message with type t = LLMChatChunk.t)
  end

end = struct
  module rec ChatMessage : sig
    type t = {
      role:string;
      (**
{%html:
<p>&quot;system&quot;, &quot;user&quot;, &quot;assistant&quot;</p>
%}
      *)

      content:string;
    }
    val make: ?role:string -> ?content:string -> unit -> t
    (** Helper function to generate a message using default values *)

    val to_proto: t -> Runtime'.Writer.t
    (** Serialize the message to binary format *)

    val from_proto: Runtime'.Reader.t -> (t, [> Runtime'.Result.error]) result
    (** Deserialize from binary format *)

    val to_json: Runtime'.Json_options.t -> t -> Runtime'.Json.t
    (** Serialize to Json (compatible with Yojson.Basic.t) *)

    val from_json: Runtime'.Json.t -> (t, [> Runtime'.Result.error]) result
    (** Deserialize from Json (compatible with Yojson.Basic.t) *)

    val name: unit -> string
    (** Fully qualified protobuf name of this message *)

    (**/**)
    type make_t = ?role:string -> ?content:string -> unit -> t
    val merge: t -> t -> t
    val to_proto': Runtime'.Writer.t -> t -> unit
    val from_proto_exn: Runtime'.Reader.t -> t
    val from_json_exn: Runtime'.Json.t -> t
    (**/**)
  end = struct
    module This'_ = ChatMessage
    let name () = ".protocol.ChatMessage"
    type t = {
      role:string;
      content:string;
    }
    type make_t = ?role:string -> ?content:string -> unit -> t
    let make ?(role = {||}) ?(content = {||}) () = { role; content }
    let merge =
    let merge_role = Runtime'.Merge.merge Runtime'.Spec.( basic ((1, "role", "role"), string, ({||})) ) in
    let merge_content = Runtime'.Merge.merge Runtime'.Spec.( basic ((2, "content", "content"), string, ({||})) ) in
    fun t1 t2 -> {
    	role = (merge_role t1.role t2.role);
    	content = (merge_content t1.content t2.content);
     }
    let spec () = Runtime'.Spec.( basic ((1, "role", "role"), string, ({||})) ^:: basic ((2, "content", "content"), string, ({||})) ^:: nil )
    let to_proto' =
      let serialize = Runtime'.apply_lazy (fun () -> Runtime'.Serialize.serialize (spec ())) in
      fun writer { role; content } -> serialize writer role content

    let to_proto t = let writer = Runtime'.Writer.init () in to_proto' writer t; writer
    let from_proto_exn =
      let constructor role content = { role; content } in
      Runtime'.apply_lazy (fun () -> Runtime'.Deserialize.deserialize (spec ()) constructor)
    let from_proto writer = Runtime'.Result.catch (fun () -> from_proto_exn writer)
    let to_json options =
      let serialize = Runtime'.Serialize_json.serialize ~message_name:(name ()) (spec ()) options in
      fun { role; content } -> serialize role content
    let from_json_exn =
      let constructor role content = { role; content } in
      Runtime'.apply_lazy (fun () -> Runtime'.Deserialize_json.deserialize ~message_name:(name ()) (spec ()) constructor)
    let from_json json = Runtime'.Result.catch (fun () -> from_json_exn json)
  end

  and LLMChatRequest : sig
    type t = {
      system_prompt:string[@ocaml.alert protobuf "Marked as deprecated in the .proto file"];
      (**
{%html:
<p>Deprecated: use messages field for new callers.</p>
%}
      *)

      user_prompt:string[@ocaml.alert protobuf "Marked as deprecated in the .proto file"];
      (**
{%html:
<p>Deprecated: use messages field for new callers.</p>
%}
      *)

      model:string;
      temperature:float;
      max_tokens:int;
      provider:string;
      attachments:Attachment.t list;
      messages:ChatMessage.t list;
      (**
{%html:
<p>Multi-turn conversation history. When populated, takes precedence
over system_prompt/user_prompt which remain for single-turn callers.</p>
%}
      *)

      priority:int;
      (**
{%html:
<p>Queuing priority. Lower value = higher priority.
0 = interactive (user-initiated), 10 = background (generators, batch).</p>
%}
      *)

    }
    val make: ?system_prompt:string -> ?user_prompt:string -> ?model:string -> ?temperature:float -> ?max_tokens:int -> ?provider:string -> ?attachments:Attachment.t list -> ?messages:ChatMessage.t list -> ?priority:int -> unit -> t
    (** Helper function to generate a message using default values *)

    val to_proto: t -> Runtime'.Writer.t
    (** Serialize the message to binary format *)

    val from_proto: Runtime'.Reader.t -> (t, [> Runtime'.Result.error]) result
    (** Deserialize from binary format *)

    val to_json: Runtime'.Json_options.t -> t -> Runtime'.Json.t
    (** Serialize to Json (compatible with Yojson.Basic.t) *)

    val from_json: Runtime'.Json.t -> (t, [> Runtime'.Result.error]) result
    (** Deserialize from Json (compatible with Yojson.Basic.t) *)

    val name: unit -> string
    (** Fully qualified protobuf name of this message *)

    (**/**)
    type make_t = ?system_prompt:string -> ?user_prompt:string -> ?model:string -> ?temperature:float -> ?max_tokens:int -> ?provider:string -> ?attachments:Attachment.t list -> ?messages:ChatMessage.t list -> ?priority:int -> unit -> t
    val merge: t -> t -> t
    val to_proto': Runtime'.Writer.t -> t -> unit
    val from_proto_exn: Runtime'.Reader.t -> t
    val from_json_exn: Runtime'.Json.t -> t
    (**/**)
  end = struct
    module This'_ = LLMChatRequest
    let name () = ".protocol.LLMChatRequest"
    type t = {
      system_prompt:string;
      user_prompt:string;
      model:string;
      temperature:float;
      max_tokens:int;
      provider:string;
      attachments:Attachment.t list;
      messages:ChatMessage.t list;
      priority:int;
    }
    type make_t = ?system_prompt:string -> ?user_prompt:string -> ?model:string -> ?temperature:float -> ?max_tokens:int -> ?provider:string -> ?attachments:Attachment.t list -> ?messages:ChatMessage.t list -> ?priority:int -> unit -> t
    let make ?(system_prompt = {||}) ?(user_prompt = {||}) ?(model = {||}) ?(temperature = 0.) ?(max_tokens = 0) ?(provider = {||}) ?(attachments = []) ?(messages = []) ?(priority = 0) () = { system_prompt; user_prompt; model; temperature; max_tokens; provider; attachments; messages; priority }
    let merge =
    let merge_system_prompt = Runtime'.Merge.merge Runtime'.Spec.( basic ((1, "system_prompt", "systemPrompt"), string, ({||})) ) in
    let merge_user_prompt = Runtime'.Merge.merge Runtime'.Spec.( basic ((2, "user_prompt", "userPrompt"), string, ({||})) ) in
    let merge_model = Runtime'.Merge.merge Runtime'.Spec.( basic ((3, "model", "model"), string, ({||})) ) in
    let merge_temperature = Runtime'.Merge.merge Runtime'.Spec.( basic ((4, "temperature", "temperature"), double, (0.)) ) in
    let merge_max_tokens = Runtime'.Merge.merge Runtime'.Spec.( basic ((5, "max_tokens", "maxTokens"), int32_int, (0)) ) in
    let merge_provider = Runtime'.Merge.merge Runtime'.Spec.( basic ((6, "provider", "provider"), string, ({||})) ) in
    let merge_attachments = Runtime'.Merge.merge Runtime'.Spec.( repeated ((7, "attachments", "attachments"), (message (module Attachment)), not_packed) ) in
    let merge_messages = Runtime'.Merge.merge Runtime'.Spec.( repeated ((8, "messages", "messages"), (message (module ChatMessage)), not_packed) ) in
    let merge_priority = Runtime'.Merge.merge Runtime'.Spec.( basic ((9, "priority", "priority"), int32_int, (0)) ) in
    fun t1 t2 -> {
    	system_prompt = (merge_system_prompt t1.system_prompt t2.system_prompt);
    	user_prompt = (merge_user_prompt t1.user_prompt t2.user_prompt);
    	model = (merge_model t1.model t2.model);
    	temperature = (merge_temperature t1.temperature t2.temperature);
    	max_tokens = (merge_max_tokens t1.max_tokens t2.max_tokens);
    	provider = (merge_provider t1.provider t2.provider);
    	attachments = (merge_attachments t1.attachments t2.attachments);
    	messages = (merge_messages t1.messages t2.messages);
    	priority = (merge_priority t1.priority t2.priority);
     }
    let spec () = Runtime'.Spec.( basic ((1, "system_prompt", "systemPrompt"), string, ({||})) ^:: basic ((2, "user_prompt", "userPrompt"), string, ({||})) ^:: basic ((3, "model", "model"), string, ({||})) ^:: basic ((4, "temperature", "temperature"), double, (0.)) ^:: basic ((5, "max_tokens", "maxTokens"), int32_int, (0)) ^:: basic ((6, "provider", "provider"), string, ({||})) ^:: repeated ((7, "attachments", "attachments"), (message (module Attachment)), not_packed) ^:: repeated ((8, "messages", "messages"), (message (module ChatMessage)), not_packed) ^:: basic ((9, "priority", "priority"), int32_int, (0)) ^:: nil )
    let to_proto' =
      let serialize = Runtime'.apply_lazy (fun () -> Runtime'.Serialize.serialize (spec ())) in
      fun writer { system_prompt; user_prompt; model; temperature; max_tokens; provider; attachments; messages; priority } -> serialize writer system_prompt user_prompt model temperature max_tokens provider attachments messages priority

    let to_proto t = let writer = Runtime'.Writer.init () in to_proto' writer t; writer
    let from_proto_exn =
      let constructor system_prompt user_prompt model temperature max_tokens provider attachments messages priority = { system_prompt; user_prompt; model; temperature; max_tokens; provider; attachments; messages; priority } in
      Runtime'.apply_lazy (fun () -> Runtime'.Deserialize.deserialize (spec ()) constructor)
    let from_proto writer = Runtime'.Result.catch (fun () -> from_proto_exn writer)
    let to_json options =
      let serialize = Runtime'.Serialize_json.serialize ~message_name:(name ()) (spec ()) options in
      fun { system_prompt; user_prompt; model; temperature; max_tokens; provider; attachments; messages; priority } -> serialize system_prompt user_prompt model temperature max_tokens provider attachments messages priority
    let from_json_exn =
      let constructor system_prompt user_prompt model temperature max_tokens provider attachments messages priority = { system_prompt; user_prompt; model; temperature; max_tokens; provider; attachments; messages; priority } in
      Runtime'.apply_lazy (fun () -> Runtime'.Deserialize_json.deserialize ~message_name:(name ()) (spec ()) constructor)
    let from_json json = Runtime'.Result.catch (fun () -> from_json_exn json)
  end

  and Attachment : sig
    type t = {
      mime_type:string;
      data:string;
      filename:string;
    }
    val make: ?mime_type:string -> ?data:string -> ?filename:string -> unit -> t
    (** Helper function to generate a message using default values *)

    val to_proto: t -> Runtime'.Writer.t
    (** Serialize the message to binary format *)

    val from_proto: Runtime'.Reader.t -> (t, [> Runtime'.Result.error]) result
    (** Deserialize from binary format *)

    val to_json: Runtime'.Json_options.t -> t -> Runtime'.Json.t
    (** Serialize to Json (compatible with Yojson.Basic.t) *)

    val from_json: Runtime'.Json.t -> (t, [> Runtime'.Result.error]) result
    (** Deserialize from Json (compatible with Yojson.Basic.t) *)

    val name: unit -> string
    (** Fully qualified protobuf name of this message *)

    (**/**)
    type make_t = ?mime_type:string -> ?data:string -> ?filename:string -> unit -> t
    val merge: t -> t -> t
    val to_proto': Runtime'.Writer.t -> t -> unit
    val from_proto_exn: Runtime'.Reader.t -> t
    val from_json_exn: Runtime'.Json.t -> t
    (**/**)
  end = struct
    module This'_ = Attachment
    let name () = ".protocol.Attachment"
    type t = {
      mime_type:string;
      data:string;
      filename:string;
    }
    type make_t = ?mime_type:string -> ?data:string -> ?filename:string -> unit -> t
    let make ?(mime_type = {||}) ?(data = {||}) ?(filename = {||}) () = { mime_type; data; filename }
    let merge =
    let merge_mime_type = Runtime'.Merge.merge Runtime'.Spec.( basic ((1, "mime_type", "mimeType"), string, ({||})) ) in
    let merge_data = Runtime'.Merge.merge Runtime'.Spec.( basic ((2, "data", "data"), string, ({||})) ) in
    let merge_filename = Runtime'.Merge.merge Runtime'.Spec.( basic ((3, "filename", "filename"), string, ({||})) ) in
    fun t1 t2 -> {
    	mime_type = (merge_mime_type t1.mime_type t2.mime_type);
    	data = (merge_data t1.data t2.data);
    	filename = (merge_filename t1.filename t2.filename);
     }
    let spec () = Runtime'.Spec.( basic ((1, "mime_type", "mimeType"), string, ({||})) ^:: basic ((2, "data", "data"), string, ({||})) ^:: basic ((3, "filename", "filename"), string, ({||})) ^:: nil )
    let to_proto' =
      let serialize = Runtime'.apply_lazy (fun () -> Runtime'.Serialize.serialize (spec ())) in
      fun writer { mime_type; data; filename } -> serialize writer mime_type data filename

    let to_proto t = let writer = Runtime'.Writer.init () in to_proto' writer t; writer
    let from_proto_exn =
      let constructor mime_type data filename = { mime_type; data; filename } in
      Runtime'.apply_lazy (fun () -> Runtime'.Deserialize.deserialize (spec ()) constructor)
    let from_proto writer = Runtime'.Result.catch (fun () -> from_proto_exn writer)
    let to_json options =
      let serialize = Runtime'.Serialize_json.serialize ~message_name:(name ()) (spec ()) options in
      fun { mime_type; data; filename } -> serialize mime_type data filename
    let from_json_exn =
      let constructor mime_type data filename = { mime_type; data; filename } in
      Runtime'.apply_lazy (fun () -> Runtime'.Deserialize_json.deserialize ~message_name:(name ()) (spec ()) constructor)
    let from_json json = Runtime'.Result.catch (fun () -> from_json_exn json)
  end

  and LLMChatResponse : sig
    type t = {
      content:string;
      model:string;
      prompt_tokens:int;
      completion_tokens:int;
      total_tokens:int;
    }
    val make: ?content:string -> ?model:string -> ?prompt_tokens:int -> ?completion_tokens:int -> ?total_tokens:int -> unit -> t
    (** Helper function to generate a message using default values *)

    val to_proto: t -> Runtime'.Writer.t
    (** Serialize the message to binary format *)

    val from_proto: Runtime'.Reader.t -> (t, [> Runtime'.Result.error]) result
    (** Deserialize from binary format *)

    val to_json: Runtime'.Json_options.t -> t -> Runtime'.Json.t
    (** Serialize to Json (compatible with Yojson.Basic.t) *)

    val from_json: Runtime'.Json.t -> (t, [> Runtime'.Result.error]) result
    (** Deserialize from Json (compatible with Yojson.Basic.t) *)

    val name: unit -> string
    (** Fully qualified protobuf name of this message *)

    (**/**)
    type make_t = ?content:string -> ?model:string -> ?prompt_tokens:int -> ?completion_tokens:int -> ?total_tokens:int -> unit -> t
    val merge: t -> t -> t
    val to_proto': Runtime'.Writer.t -> t -> unit
    val from_proto_exn: Runtime'.Reader.t -> t
    val from_json_exn: Runtime'.Json.t -> t
    (**/**)
  end = struct
    module This'_ = LLMChatResponse
    let name () = ".protocol.LLMChatResponse"
    type t = {
      content:string;
      model:string;
      prompt_tokens:int;
      completion_tokens:int;
      total_tokens:int;
    }
    type make_t = ?content:string -> ?model:string -> ?prompt_tokens:int -> ?completion_tokens:int -> ?total_tokens:int -> unit -> t
    let make ?(content = {||}) ?(model = {||}) ?(prompt_tokens = 0) ?(completion_tokens = 0) ?(total_tokens = 0) () = { content; model; prompt_tokens; completion_tokens; total_tokens }
    let merge =
    let merge_content = Runtime'.Merge.merge Runtime'.Spec.( basic ((1, "content", "content"), string, ({||})) ) in
    let merge_model = Runtime'.Merge.merge Runtime'.Spec.( basic ((2, "model", "model"), string, ({||})) ) in
    let merge_prompt_tokens = Runtime'.Merge.merge Runtime'.Spec.( basic ((3, "prompt_tokens", "promptTokens"), int32_int, (0)) ) in
    let merge_completion_tokens = Runtime'.Merge.merge Runtime'.Spec.( basic ((4, "completion_tokens", "completionTokens"), int32_int, (0)) ) in
    let merge_total_tokens = Runtime'.Merge.merge Runtime'.Spec.( basic ((5, "total_tokens", "totalTokens"), int32_int, (0)) ) in
    fun t1 t2 -> {
    	content = (merge_content t1.content t2.content);
    	model = (merge_model t1.model t2.model);
    	prompt_tokens = (merge_prompt_tokens t1.prompt_tokens t2.prompt_tokens);
    	completion_tokens = (merge_completion_tokens t1.completion_tokens t2.completion_tokens);
    	total_tokens = (merge_total_tokens t1.total_tokens t2.total_tokens);
     }
    let spec () = Runtime'.Spec.( basic ((1, "content", "content"), string, ({||})) ^:: basic ((2, "model", "model"), string, ({||})) ^:: basic ((3, "prompt_tokens", "promptTokens"), int32_int, (0)) ^:: basic ((4, "completion_tokens", "completionTokens"), int32_int, (0)) ^:: basic ((5, "total_tokens", "totalTokens"), int32_int, (0)) ^:: nil )
    let to_proto' =
      let serialize = Runtime'.apply_lazy (fun () -> Runtime'.Serialize.serialize (spec ())) in
      fun writer { content; model; prompt_tokens; completion_tokens; total_tokens } -> serialize writer content model prompt_tokens completion_tokens total_tokens

    let to_proto t = let writer = Runtime'.Writer.init () in to_proto' writer t; writer
    let from_proto_exn =
      let constructor content model prompt_tokens completion_tokens total_tokens = { content; model; prompt_tokens; completion_tokens; total_tokens } in
      Runtime'.apply_lazy (fun () -> Runtime'.Deserialize.deserialize (spec ()) constructor)
    let from_proto writer = Runtime'.Result.catch (fun () -> from_proto_exn writer)
    let to_json options =
      let serialize = Runtime'.Serialize_json.serialize ~message_name:(name ()) (spec ()) options in
      fun { content; model; prompt_tokens; completion_tokens; total_tokens } -> serialize content model prompt_tokens completion_tokens total_tokens
    let from_json_exn =
      let constructor content model prompt_tokens completion_tokens total_tokens = { content; model; prompt_tokens; completion_tokens; total_tokens } in
      Runtime'.apply_lazy (fun () -> Runtime'.Deserialize_json.deserialize ~message_name:(name ()) (spec ()) constructor)
    let from_json json = Runtime'.Result.catch (fun () -> from_json_exn json)
  end

  and LLMChatChunk : sig
    type t = {
      token:string;
      (**
{%html:
<p>The generated token text</p>
%}
      *)

      done':bool;
      (**
{%html:
<p>True on final chunk</p>
%}
      *)

      model:string;
      (**
{%html:
<p>Model name (sent on first or last chunk)</p>
%}
      *)

      prompt_tokens:int;
      (**
{%html:
<p>Sent on final chunk</p>
%}
      *)

      completion_tokens:int;
      (**
{%html:
<p>Sent on final chunk</p>
%}
      *)

      total_tokens:int;
      (**
{%html:
<p>Sent on final chunk</p>
%}
      *)

      signal:TokenSignalProto.t option;
      (**
{%html:
<p>Per-token signal data (if available)</p>
%}
      *)

    }
    val make: ?token:string -> ?done':bool -> ?model:string -> ?prompt_tokens:int -> ?completion_tokens:int -> ?total_tokens:int -> ?signal:TokenSignalProto.t -> unit -> t
    (** Helper function to generate a message using default values *)

    val to_proto: t -> Runtime'.Writer.t
    (** Serialize the message to binary format *)

    val from_proto: Runtime'.Reader.t -> (t, [> Runtime'.Result.error]) result
    (** Deserialize from binary format *)

    val to_json: Runtime'.Json_options.t -> t -> Runtime'.Json.t
    (** Serialize to Json (compatible with Yojson.Basic.t) *)

    val from_json: Runtime'.Json.t -> (t, [> Runtime'.Result.error]) result
    (** Deserialize from Json (compatible with Yojson.Basic.t) *)

    val name: unit -> string
    (** Fully qualified protobuf name of this message *)

    (**/**)
    type make_t = ?token:string -> ?done':bool -> ?model:string -> ?prompt_tokens:int -> ?completion_tokens:int -> ?total_tokens:int -> ?signal:TokenSignalProto.t -> unit -> t
    val merge: t -> t -> t
    val to_proto': Runtime'.Writer.t -> t -> unit
    val from_proto_exn: Runtime'.Reader.t -> t
    val from_json_exn: Runtime'.Json.t -> t
    (**/**)
  end = struct
    module This'_ = LLMChatChunk
    let name () = ".protocol.LLMChatChunk"
    type t = {
      token:string;
      done':bool;
      model:string;
      prompt_tokens:int;
      completion_tokens:int;
      total_tokens:int;
      signal:TokenSignalProto.t option;
    }
    type make_t = ?token:string -> ?done':bool -> ?model:string -> ?prompt_tokens:int -> ?completion_tokens:int -> ?total_tokens:int -> ?signal:TokenSignalProto.t -> unit -> t
    let make ?(token = {||}) ?(done' = false) ?(model = {||}) ?(prompt_tokens = 0) ?(completion_tokens = 0) ?(total_tokens = 0) ?signal () = { token; done'; model; prompt_tokens; completion_tokens; total_tokens; signal }
    let merge =
    let merge_token = Runtime'.Merge.merge Runtime'.Spec.( basic ((1, "token", "token"), string, ({||})) ) in
    let merge_done' = Runtime'.Merge.merge Runtime'.Spec.( basic ((2, "done", "done"), bool, (false)) ) in
    let merge_model = Runtime'.Merge.merge Runtime'.Spec.( basic ((3, "model", "model"), string, ({||})) ) in
    let merge_prompt_tokens = Runtime'.Merge.merge Runtime'.Spec.( basic ((4, "prompt_tokens", "promptTokens"), int32_int, (0)) ) in
    let merge_completion_tokens = Runtime'.Merge.merge Runtime'.Spec.( basic ((5, "completion_tokens", "completionTokens"), int32_int, (0)) ) in
    let merge_total_tokens = Runtime'.Merge.merge Runtime'.Spec.( basic ((6, "total_tokens", "totalTokens"), int32_int, (0)) ) in
    let merge_signal = Runtime'.Merge.merge Runtime'.Spec.( basic_opt ((7, "signal", "signal"), (message (module TokenSignalProto))) ) in
    fun t1 t2 -> {
    	token = (merge_token t1.token t2.token);
    	done' = (merge_done' t1.done' t2.done');
    	model = (merge_model t1.model t2.model);
    	prompt_tokens = (merge_prompt_tokens t1.prompt_tokens t2.prompt_tokens);
    	completion_tokens = (merge_completion_tokens t1.completion_tokens t2.completion_tokens);
    	total_tokens = (merge_total_tokens t1.total_tokens t2.total_tokens);
    	signal = (merge_signal t1.signal t2.signal);
     }
    let spec () = Runtime'.Spec.( basic ((1, "token", "token"), string, ({||})) ^:: basic ((2, "done", "done"), bool, (false)) ^:: basic ((3, "model", "model"), string, ({||})) ^:: basic ((4, "prompt_tokens", "promptTokens"), int32_int, (0)) ^:: basic ((5, "completion_tokens", "completionTokens"), int32_int, (0)) ^:: basic ((6, "total_tokens", "totalTokens"), int32_int, (0)) ^:: basic_opt ((7, "signal", "signal"), (message (module TokenSignalProto))) ^:: nil )
    let to_proto' =
      let serialize = Runtime'.apply_lazy (fun () -> Runtime'.Serialize.serialize (spec ())) in
      fun writer { token; done'; model; prompt_tokens; completion_tokens; total_tokens; signal } -> serialize writer token done' model prompt_tokens completion_tokens total_tokens signal

    let to_proto t = let writer = Runtime'.Writer.init () in to_proto' writer t; writer
    let from_proto_exn =
      let constructor token done' model prompt_tokens completion_tokens total_tokens signal = { token; done'; model; prompt_tokens; completion_tokens; total_tokens; signal } in
      Runtime'.apply_lazy (fun () -> Runtime'.Deserialize.deserialize (spec ()) constructor)
    let from_proto writer = Runtime'.Result.catch (fun () -> from_proto_exn writer)
    let to_json options =
      let serialize = Runtime'.Serialize_json.serialize ~message_name:(name ()) (spec ()) options in
      fun { token; done'; model; prompt_tokens; completion_tokens; total_tokens; signal } -> serialize token done' model prompt_tokens completion_tokens total_tokens signal
    let from_json_exn =
      let constructor token done' model prompt_tokens completion_tokens total_tokens signal = { token; done'; model; prompt_tokens; completion_tokens; total_tokens; signal } in
      Runtime'.apply_lazy (fun () -> Runtime'.Deserialize_json.deserialize ~message_name:(name ()) (spec ()) constructor)
    let from_json json = Runtime'.Result.catch (fun () -> from_json_exn json)
  end

  and TokenSignalProto : sig
    type t = {
      confidence:float;
      (**
{%html:
<p>P(chosen) from raw distribution</p>
%}
      *)

      entropy:float;
      (**
{%html:
<p>Shannon entropy in bits</p>
%}
      *)

      top_gap:float;
      (**
{%html:
<p>P(top1) - P(top2)</p>
%}
      *)

      top_k:TokenCandidateProto.t list;
      (**
{%html:
<p>Top-k candidates</p>
%}
      *)

      full_distribution:float list;
      (**
{%html:
<p>Full softmax distribution (vocab_size floats)</p>
%}
      *)

      sampler_stages:SamplerStageProto.t list;
      (**
{%html:
<p>Per-stage snapshots through sampler chain</p>
%}
      *)

    }
    val make: ?confidence:float -> ?entropy:float -> ?top_gap:float -> ?top_k:TokenCandidateProto.t list -> ?full_distribution:float list -> ?sampler_stages:SamplerStageProto.t list -> unit -> t
    (** Helper function to generate a message using default values *)

    val to_proto: t -> Runtime'.Writer.t
    (** Serialize the message to binary format *)

    val from_proto: Runtime'.Reader.t -> (t, [> Runtime'.Result.error]) result
    (** Deserialize from binary format *)

    val to_json: Runtime'.Json_options.t -> t -> Runtime'.Json.t
    (** Serialize to Json (compatible with Yojson.Basic.t) *)

    val from_json: Runtime'.Json.t -> (t, [> Runtime'.Result.error]) result
    (** Deserialize from Json (compatible with Yojson.Basic.t) *)

    val name: unit -> string
    (** Fully qualified protobuf name of this message *)

    (**/**)
    type make_t = ?confidence:float -> ?entropy:float -> ?top_gap:float -> ?top_k:TokenCandidateProto.t list -> ?full_distribution:float list -> ?sampler_stages:SamplerStageProto.t list -> unit -> t
    val merge: t -> t -> t
    val to_proto': Runtime'.Writer.t -> t -> unit
    val from_proto_exn: Runtime'.Reader.t -> t
    val from_json_exn: Runtime'.Json.t -> t
    (**/**)
  end = struct
    module This'_ = TokenSignalProto
    let name () = ".protocol.TokenSignalProto"
    type t = {
      confidence:float;
      entropy:float;
      top_gap:float;
      top_k:TokenCandidateProto.t list;
      full_distribution:float list;
      sampler_stages:SamplerStageProto.t list;
    }
    type make_t = ?confidence:float -> ?entropy:float -> ?top_gap:float -> ?top_k:TokenCandidateProto.t list -> ?full_distribution:float list -> ?sampler_stages:SamplerStageProto.t list -> unit -> t
    let make ?(confidence = 0.) ?(entropy = 0.) ?(top_gap = 0.) ?(top_k = []) ?(full_distribution = []) ?(sampler_stages = []) () = { confidence; entropy; top_gap; top_k; full_distribution; sampler_stages }
    let merge =
    let merge_confidence = Runtime'.Merge.merge Runtime'.Spec.( basic ((1, "confidence", "confidence"), float, (0.)) ) in
    let merge_entropy = Runtime'.Merge.merge Runtime'.Spec.( basic ((2, "entropy", "entropy"), float, (0.)) ) in
    let merge_top_gap = Runtime'.Merge.merge Runtime'.Spec.( basic ((3, "top_gap", "topGap"), float, (0.)) ) in
    let merge_top_k = Runtime'.Merge.merge Runtime'.Spec.( repeated ((4, "top_k", "topK"), (message (module TokenCandidateProto)), not_packed) ) in
    let merge_full_distribution = Runtime'.Merge.merge Runtime'.Spec.( repeated ((5, "full_distribution", "fullDistribution"), float, packed) ) in
    let merge_sampler_stages = Runtime'.Merge.merge Runtime'.Spec.( repeated ((6, "sampler_stages", "samplerStages"), (message (module SamplerStageProto)), not_packed) ) in
    fun t1 t2 -> {
    	confidence = (merge_confidence t1.confidence t2.confidence);
    	entropy = (merge_entropy t1.entropy t2.entropy);
    	top_gap = (merge_top_gap t1.top_gap t2.top_gap);
    	top_k = (merge_top_k t1.top_k t2.top_k);
    	full_distribution = (merge_full_distribution t1.full_distribution t2.full_distribution);
    	sampler_stages = (merge_sampler_stages t1.sampler_stages t2.sampler_stages);
     }
    let spec () = Runtime'.Spec.( basic ((1, "confidence", "confidence"), float, (0.)) ^:: basic ((2, "entropy", "entropy"), float, (0.)) ^:: basic ((3, "top_gap", "topGap"), float, (0.)) ^:: repeated ((4, "top_k", "topK"), (message (module TokenCandidateProto)), not_packed) ^:: repeated ((5, "full_distribution", "fullDistribution"), float, packed) ^:: repeated ((6, "sampler_stages", "samplerStages"), (message (module SamplerStageProto)), not_packed) ^:: nil )
    let to_proto' =
      let serialize = Runtime'.apply_lazy (fun () -> Runtime'.Serialize.serialize (spec ())) in
      fun writer { confidence; entropy; top_gap; top_k; full_distribution; sampler_stages } -> serialize writer confidence entropy top_gap top_k full_distribution sampler_stages

    let to_proto t = let writer = Runtime'.Writer.init () in to_proto' writer t; writer
    let from_proto_exn =
      let constructor confidence entropy top_gap top_k full_distribution sampler_stages = { confidence; entropy; top_gap; top_k; full_distribution; sampler_stages } in
      Runtime'.apply_lazy (fun () -> Runtime'.Deserialize.deserialize (spec ()) constructor)
    let from_proto writer = Runtime'.Result.catch (fun () -> from_proto_exn writer)
    let to_json options =
      let serialize = Runtime'.Serialize_json.serialize ~message_name:(name ()) (spec ()) options in
      fun { confidence; entropy; top_gap; top_k; full_distribution; sampler_stages } -> serialize confidence entropy top_gap top_k full_distribution sampler_stages
    let from_json_exn =
      let constructor confidence entropy top_gap top_k full_distribution sampler_stages = { confidence; entropy; top_gap; top_k; full_distribution; sampler_stages } in
      Runtime'.apply_lazy (fun () -> Runtime'.Deserialize_json.deserialize ~message_name:(name ()) (spec ()) constructor)
    let from_json json = Runtime'.Result.catch (fun () -> from_json_exn json)
  end

  and TokenCandidateProto : sig
    type t = {
      id:int;
      text:string;
      prob:float;
    }
    val make: ?id:int -> ?text:string -> ?prob:float -> unit -> t
    (** Helper function to generate a message using default values *)

    val to_proto: t -> Runtime'.Writer.t
    (** Serialize the message to binary format *)

    val from_proto: Runtime'.Reader.t -> (t, [> Runtime'.Result.error]) result
    (** Deserialize from binary format *)

    val to_json: Runtime'.Json_options.t -> t -> Runtime'.Json.t
    (** Serialize to Json (compatible with Yojson.Basic.t) *)

    val from_json: Runtime'.Json.t -> (t, [> Runtime'.Result.error]) result
    (** Deserialize from Json (compatible with Yojson.Basic.t) *)

    val name: unit -> string
    (** Fully qualified protobuf name of this message *)

    (**/**)
    type make_t = ?id:int -> ?text:string -> ?prob:float -> unit -> t
    val merge: t -> t -> t
    val to_proto': Runtime'.Writer.t -> t -> unit
    val from_proto_exn: Runtime'.Reader.t -> t
    val from_json_exn: Runtime'.Json.t -> t
    (**/**)
  end = struct
    module This'_ = TokenCandidateProto
    let name () = ".protocol.TokenCandidateProto"
    type t = {
      id:int;
      text:string;
      prob:float;
    }
    type make_t = ?id:int -> ?text:string -> ?prob:float -> unit -> t
    let make ?(id = 0) ?(text = {||}) ?(prob = 0.) () = { id; text; prob }
    let merge =
    let merge_id = Runtime'.Merge.merge Runtime'.Spec.( basic ((1, "id", "id"), int32_int, (0)) ) in
    let merge_text = Runtime'.Merge.merge Runtime'.Spec.( basic ((2, "text", "text"), string, ({||})) ) in
    let merge_prob = Runtime'.Merge.merge Runtime'.Spec.( basic ((3, "prob", "prob"), float, (0.)) ) in
    fun t1 t2 -> {
    	id = (merge_id t1.id t2.id);
    	text = (merge_text t1.text t2.text);
    	prob = (merge_prob t1.prob t2.prob);
     }
    let spec () = Runtime'.Spec.( basic ((1, "id", "id"), int32_int, (0)) ^:: basic ((2, "text", "text"), string, ({||})) ^:: basic ((3, "prob", "prob"), float, (0.)) ^:: nil )
    let to_proto' =
      let serialize = Runtime'.apply_lazy (fun () -> Runtime'.Serialize.serialize (spec ())) in
      fun writer { id; text; prob } -> serialize writer id text prob

    let to_proto t = let writer = Runtime'.Writer.init () in to_proto' writer t; writer
    let from_proto_exn =
      let constructor id text prob = { id; text; prob } in
      Runtime'.apply_lazy (fun () -> Runtime'.Deserialize.deserialize (spec ()) constructor)
    let from_proto writer = Runtime'.Result.catch (fun () -> from_proto_exn writer)
    let to_json options =
      let serialize = Runtime'.Serialize_json.serialize ~message_name:(name ()) (spec ()) options in
      fun { id; text; prob } -> serialize id text prob
    let from_json_exn =
      let constructor id text prob = { id; text; prob } in
      Runtime'.apply_lazy (fun () -> Runtime'.Deserialize_json.deserialize ~message_name:(name ()) (spec ()) constructor)
    let from_json json = Runtime'.Result.catch (fun () -> from_json_exn json)
  end

  and SamplerStageProto : sig
    type t = {
      name:string;
      (**
{%html:
<p>Stage name: &quot;logits&quot;, &quot;top_k&quot;, &quot;top_p&quot;, &quot;temp&quot;, etc.</p>
%}
      *)

      active_count:int;
      (**
{%html:
<p>Tokens remaining with nonzero probability</p>
%}
      *)

      top1_prob:float;
      (**
{%html:
<p>P(top token) after this stage</p>
%}
      *)

      entropy:float;
      (**
{%html:
<p>Shannon entropy after this stage</p>
%}
      *)

      top_k:TokenCandidateProto.t list;
      (**
{%html:
<p>Top-5 candidates after this stage</p>
%}
      *)

    }
    val make: ?name:string -> ?active_count:int -> ?top1_prob:float -> ?entropy:float -> ?top_k:TokenCandidateProto.t list -> unit -> t
    (** Helper function to generate a message using default values *)

    val to_proto: t -> Runtime'.Writer.t
    (** Serialize the message to binary format *)

    val from_proto: Runtime'.Reader.t -> (t, [> Runtime'.Result.error]) result
    (** Deserialize from binary format *)

    val to_json: Runtime'.Json_options.t -> t -> Runtime'.Json.t
    (** Serialize to Json (compatible with Yojson.Basic.t) *)

    val from_json: Runtime'.Json.t -> (t, [> Runtime'.Result.error]) result
    (** Deserialize from Json (compatible with Yojson.Basic.t) *)

    val name: unit -> string
    (** Fully qualified protobuf name of this message *)

    (**/**)
    type make_t = ?name:string -> ?active_count:int -> ?top1_prob:float -> ?entropy:float -> ?top_k:TokenCandidateProto.t list -> unit -> t
    val merge: t -> t -> t
    val to_proto': Runtime'.Writer.t -> t -> unit
    val from_proto_exn: Runtime'.Reader.t -> t
    val from_json_exn: Runtime'.Json.t -> t
    (**/**)
  end = struct
    module This'_ = SamplerStageProto
    let name () = ".protocol.SamplerStageProto"
    type t = {
      name:string;
      active_count:int;
      top1_prob:float;
      entropy:float;
      top_k:TokenCandidateProto.t list;
    }
    type make_t = ?name:string -> ?active_count:int -> ?top1_prob:float -> ?entropy:float -> ?top_k:TokenCandidateProto.t list -> unit -> t
    let make ?(name = {||}) ?(active_count = 0) ?(top1_prob = 0.) ?(entropy = 0.) ?(top_k = []) () = { name; active_count; top1_prob; entropy; top_k }
    let merge =
    let merge_name = Runtime'.Merge.merge Runtime'.Spec.( basic ((1, "name", "name"), string, ({||})) ) in
    let merge_active_count = Runtime'.Merge.merge Runtime'.Spec.( basic ((2, "active_count", "activeCount"), int32_int, (0)) ) in
    let merge_top1_prob = Runtime'.Merge.merge Runtime'.Spec.( basic ((3, "top1_prob", "top1Prob"), float, (0.)) ) in
    let merge_entropy = Runtime'.Merge.merge Runtime'.Spec.( basic ((4, "entropy", "entropy"), float, (0.)) ) in
    let merge_top_k = Runtime'.Merge.merge Runtime'.Spec.( repeated ((5, "top_k", "topK"), (message (module TokenCandidateProto)), not_packed) ) in
    fun t1 t2 -> {
    	name = (merge_name t1.name t2.name);
    	active_count = (merge_active_count t1.active_count t2.active_count);
    	top1_prob = (merge_top1_prob t1.top1_prob t2.top1_prob);
    	entropy = (merge_entropy t1.entropy t2.entropy);
    	top_k = (merge_top_k t1.top_k t2.top_k);
     }
    let spec () = Runtime'.Spec.( basic ((1, "name", "name"), string, ({||})) ^:: basic ((2, "active_count", "activeCount"), int32_int, (0)) ^:: basic ((3, "top1_prob", "top1Prob"), float, (0.)) ^:: basic ((4, "entropy", "entropy"), float, (0.)) ^:: repeated ((5, "top_k", "topK"), (message (module TokenCandidateProto)), not_packed) ^:: nil )
    let to_proto' =
      let serialize = Runtime'.apply_lazy (fun () -> Runtime'.Serialize.serialize (spec ())) in
      fun writer { name; active_count; top1_prob; entropy; top_k } -> serialize writer name active_count top1_prob entropy top_k

    let to_proto t = let writer = Runtime'.Writer.init () in to_proto' writer t; writer
    let from_proto_exn =
      let constructor name active_count top1_prob entropy top_k = { name; active_count; top1_prob; entropy; top_k } in
      Runtime'.apply_lazy (fun () -> Runtime'.Deserialize.deserialize (spec ()) constructor)
    let from_proto writer = Runtime'.Result.catch (fun () -> from_proto_exn writer)
    let to_json options =
      let serialize = Runtime'.Serialize_json.serialize ~message_name:(name ()) (spec ()) options in
      fun { name; active_count; top1_prob; entropy; top_k } -> serialize name active_count top1_prob entropy top_k
    let from_json_exn =
      let constructor name active_count top1_prob entropy top_k = { name; active_count; top1_prob; entropy; top_k } in
      Runtime'.apply_lazy (fun () -> Runtime'.Deserialize_json.deserialize ~message_name:(name ()) (spec ()) constructor)
    let from_json json = Runtime'.Result.catch (fun () -> from_json_exn json)
  end

  module LLMService = struct
    module Chat = struct
      let package_name = Some "protocol"
      let service_name = "LLMService"
      let method_name = "Chat"
      let name = "/protocol.LLMService/Chat"
      module Request = LLMChatRequest
      module Response = LLMChatResponse
    end

    let chat =
      (module LLMChatRequest : Runtime'.Spec.Message with type t = LLMChatRequest.t ),
      (module LLMChatResponse : Runtime'.Spec.Message with type t = LLMChatResponse.t )

    module StreamChat = struct
      let package_name = Some "protocol"
      let service_name = "LLMService"
      let method_name = "StreamChat"
      let name = "/protocol.LLMService/StreamChat"
      module Request = LLMChatRequest
      module Response = LLMChatChunk
    end

    let streamChat =
      (module LLMChatRequest : Runtime'.Spec.Message with type t = LLMChatRequest.t ),
      (module LLMChatChunk : Runtime'.Spec.Message with type t = LLMChatChunk.t )

  end

end

