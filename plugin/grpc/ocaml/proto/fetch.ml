(********************************************************)
(*           AUTOGENERATED FILE - DO NOT EDIT!          *)
(********************************************************)
(* Generated by: ocaml-protoc-plugin                    *)
(* https://github.com/andersfugmann/ocaml-protoc-plugin *)
(********************************************************)
(*
  Source: plugin/grpc/protocol/fetch.proto
  Syntax: proto3
  Parameters:
    debug=false
    annot=''
    opens=[]
    int64_as_int=true
    int32_as_int=true
    fixed_as_int=false
    singleton_record=false
    prefix_output_with_package=false
*)
[@@@ocaml.alert "-protobuf"] (* Disable deprecation warnings for protobuf*)
(**/**)
module Runtime' = Ocaml_protoc_plugin [@@warning "-33"]
module Imported'modules = struct
end
(**/**)
module rec Protocol : sig
  module rec FetchRequest : sig
    type t = {
      auth_token:string;
      url:string;
      (**
{%html:
<p>URL to fetch</p>
%}
      *)

      subjects:string list;
      (**
{%html:
<p>Attestation subjects</p>
%}
      *)

      predicate:string;
      (**
{%html:
<p>Attestation predicate (e.g. &quot;http:get&quot;)</p>
%}
      *)

      context:string;
      (**
{%html:
<p>Attestation context (e.g. the URL itself)</p>
%}
      *)

      fresh:bool;
      (**
{%html:
<p>Skip cache, always fetch from remote</p>
%}
      *)

      actor:string;
      (**
{%html:
<p>Actor identity for the attestation (e.g. &quot;myplugin:pipeline&quot;)</p>
%}
      *)

      source:string;
      (**
{%html:
<p>Plugin name requesting the fetch. Used as attestation source and for automatic version resolution.</p>
%}
      *)

    }
    val make: ?auth_token:string -> ?url:string -> ?subjects:string list -> ?predicate:string -> ?context:string -> ?fresh:bool -> ?actor:string -> ?source:string -> unit -> t
    (** Helper function to generate a message using default values *)

    val to_proto: t -> Runtime'.Writer.t
    (** Serialize the message to binary format *)

    val from_proto: Runtime'.Reader.t -> (t, [> Runtime'.Result.error]) result
    (** Deserialize from binary format *)

    val to_json: Runtime'.Json_options.t -> t -> Runtime'.Json.t
    (** Serialize to Json (compatible with Yojson.Basic.t) *)

    val from_json: Runtime'.Json.t -> (t, [> Runtime'.Result.error]) result
    (** Deserialize from Json (compatible with Yojson.Basic.t) *)

    val name: unit -> string
    (** Fully qualified protobuf name of this message *)

    (**/**)
    type make_t = ?auth_token:string -> ?url:string -> ?subjects:string list -> ?predicate:string -> ?context:string -> ?fresh:bool -> ?actor:string -> ?source:string -> unit -> t
    val merge: t -> t -> t
    val to_proto': Runtime'.Writer.t -> t -> unit
    val from_proto_exn: Runtime'.Reader.t -> t
    val from_json_exn: Runtime'.Json.t -> t
    (**/**)
  end

  and FetchResponse : sig
    type t = {
      success:bool;
      error:string;
      body:string;
      (**
{%html:
<p>Response body</p>
%}
      *)

      status_code:int;
      (**
{%html:
<p>HTTP status code</p>
%}
      *)

      attestation_id:string;
      (**
{%html:
<p>ID of created attestation</p>
%}
      *)

    }
    val make: ?success:bool -> ?error:string -> ?body:string -> ?status_code:int -> ?attestation_id:string -> unit -> t
    (** Helper function to generate a message using default values *)

    val to_proto: t -> Runtime'.Writer.t
    (** Serialize the message to binary format *)

    val from_proto: Runtime'.Reader.t -> (t, [> Runtime'.Result.error]) result
    (** Deserialize from binary format *)

    val to_json: Runtime'.Json_options.t -> t -> Runtime'.Json.t
    (** Serialize to Json (compatible with Yojson.Basic.t) *)

    val from_json: Runtime'.Json.t -> (t, [> Runtime'.Result.error]) result
    (** Deserialize from Json (compatible with Yojson.Basic.t) *)

    val name: unit -> string
    (** Fully qualified protobuf name of this message *)

    (**/**)
    type make_t = ?success:bool -> ?error:string -> ?body:string -> ?status_code:int -> ?attestation_id:string -> unit -> t
    val merge: t -> t -> t
    val to_proto': Runtime'.Writer.t -> t -> unit
    val from_proto_exn: Runtime'.Reader.t -> t
    val from_json_exn: Runtime'.Json.t -> t
    (**/**)
  end

  module FetchService : sig
    module Fetch : sig
      include Runtime'.Service.Rpc with type Request.t = FetchRequest.t and type Response.t = FetchResponse.t
      module Request : Runtime'.Spec.Message with type t = FetchRequest.t and type make_t = FetchRequest.make_t
      (** Module alias for the request message for this method call *)

      module Response : Runtime'.Spec.Message with type t = FetchResponse.t and type make_t = FetchResponse.make_t
      (** Module alias for the response message for this method call *)

    end

    val fetch : (module Runtime'.Spec.Message with type t = FetchRequest.t) * (module Runtime'.Spec.Message with type t = FetchResponse.t)
  end

end = struct
  module rec FetchRequest : sig
    type t = {
      auth_token:string;
      url:string;
      (**
{%html:
<p>URL to fetch</p>
%}
      *)

      subjects:string list;
      (**
{%html:
<p>Attestation subjects</p>
%}
      *)

      predicate:string;
      (**
{%html:
<p>Attestation predicate (e.g. &quot;http:get&quot;)</p>
%}
      *)

      context:string;
      (**
{%html:
<p>Attestation context (e.g. the URL itself)</p>
%}
      *)

      fresh:bool;
      (**
{%html:
<p>Skip cache, always fetch from remote</p>
%}
      *)

      actor:string;
      (**
{%html:
<p>Actor identity for the attestation (e.g. &quot;myplugin:pipeline&quot;)</p>
%}
      *)

      source:string;
      (**
{%html:
<p>Plugin name requesting the fetch. Used as attestation source and for automatic version resolution.</p>
%}
      *)

    }
    val make: ?auth_token:string -> ?url:string -> ?subjects:string list -> ?predicate:string -> ?context:string -> ?fresh:bool -> ?actor:string -> ?source:string -> unit -> t
    (** Helper function to generate a message using default values *)

    val to_proto: t -> Runtime'.Writer.t
    (** Serialize the message to binary format *)

    val from_proto: Runtime'.Reader.t -> (t, [> Runtime'.Result.error]) result
    (** Deserialize from binary format *)

    val to_json: Runtime'.Json_options.t -> t -> Runtime'.Json.t
    (** Serialize to Json (compatible with Yojson.Basic.t) *)

    val from_json: Runtime'.Json.t -> (t, [> Runtime'.Result.error]) result
    (** Deserialize from Json (compatible with Yojson.Basic.t) *)

    val name: unit -> string
    (** Fully qualified protobuf name of this message *)

    (**/**)
    type make_t = ?auth_token:string -> ?url:string -> ?subjects:string list -> ?predicate:string -> ?context:string -> ?fresh:bool -> ?actor:string -> ?source:string -> unit -> t
    val merge: t -> t -> t
    val to_proto': Runtime'.Writer.t -> t -> unit
    val from_proto_exn: Runtime'.Reader.t -> t
    val from_json_exn: Runtime'.Json.t -> t
    (**/**)
  end = struct
    module This'_ = FetchRequest
    let name () = ".protocol.FetchRequest"
    type t = {
      auth_token:string;
      url:string;
      subjects:string list;
      predicate:string;
      context:string;
      fresh:bool;
      actor:string;
      source:string;
    }
    type make_t = ?auth_token:string -> ?url:string -> ?subjects:string list -> ?predicate:string -> ?context:string -> ?fresh:bool -> ?actor:string -> ?source:string -> unit -> t
    let make ?(auth_token = {||}) ?(url = {||}) ?(subjects = []) ?(predicate = {||}) ?(context = {||}) ?(fresh = false) ?(actor = {||}) ?(source = {||}) () = { auth_token; url; subjects; predicate; context; fresh; actor; source }
    let merge =
    let merge_auth_token = Runtime'.Merge.merge Runtime'.Spec.( basic ((1, "auth_token", "authToken"), string, ({||})) ) in
    let merge_url = Runtime'.Merge.merge Runtime'.Spec.( basic ((2, "url", "url"), string, ({||})) ) in
    let merge_subjects = Runtime'.Merge.merge Runtime'.Spec.( repeated ((3, "subjects", "subjects"), string, not_packed) ) in
    let merge_predicate = Runtime'.Merge.merge Runtime'.Spec.( basic ((4, "predicate", "predicate"), string, ({||})) ) in
    let merge_context = Runtime'.Merge.merge Runtime'.Spec.( basic ((5, "context", "context"), string, ({||})) ) in
    let merge_fresh = Runtime'.Merge.merge Runtime'.Spec.( basic ((6, "fresh", "fresh"), bool, (false)) ) in
    let merge_actor = Runtime'.Merge.merge Runtime'.Spec.( basic ((7, "actor", "actor"), string, ({||})) ) in
    let merge_source = Runtime'.Merge.merge Runtime'.Spec.( basic ((8, "source", "source"), string, ({||})) ) in
    fun t1 t2 -> {
    	auth_token = (merge_auth_token t1.auth_token t2.auth_token);
    	url = (merge_url t1.url t2.url);
    	subjects = (merge_subjects t1.subjects t2.subjects);
    	predicate = (merge_predicate t1.predicate t2.predicate);
    	context = (merge_context t1.context t2.context);
    	fresh = (merge_fresh t1.fresh t2.fresh);
    	actor = (merge_actor t1.actor t2.actor);
    	source = (merge_source t1.source t2.source);
     }
    let spec () = Runtime'.Spec.( basic ((1, "auth_token", "authToken"), string, ({||})) ^:: basic ((2, "url", "url"), string, ({||})) ^:: repeated ((3, "subjects", "subjects"), string, not_packed) ^:: basic ((4, "predicate", "predicate"), string, ({||})) ^:: basic ((5, "context", "context"), string, ({||})) ^:: basic ((6, "fresh", "fresh"), bool, (false)) ^:: basic ((7, "actor", "actor"), string, ({||})) ^:: basic ((8, "source", "source"), string, ({||})) ^:: nil )
    let to_proto' =
      let serialize = Runtime'.apply_lazy (fun () -> Runtime'.Serialize.serialize (spec ())) in
      fun writer { auth_token; url; subjects; predicate; context; fresh; actor; source } -> serialize writer auth_token url subjects predicate context fresh actor source

    let to_proto t = let writer = Runtime'.Writer.init () in to_proto' writer t; writer
    let from_proto_exn =
      let constructor auth_token url subjects predicate context fresh actor source = { auth_token; url; subjects; predicate; context; fresh; actor; source } in
      Runtime'.apply_lazy (fun () -> Runtime'.Deserialize.deserialize (spec ()) constructor)
    let from_proto writer = Runtime'.Result.catch (fun () -> from_proto_exn writer)
    let to_json options =
      let serialize = Runtime'.Serialize_json.serialize ~message_name:(name ()) (spec ()) options in
      fun { auth_token; url; subjects; predicate; context; fresh; actor; source } -> serialize auth_token url subjects predicate context fresh actor source
    let from_json_exn =
      let constructor auth_token url subjects predicate context fresh actor source = { auth_token; url; subjects; predicate; context; fresh; actor; source } in
      Runtime'.apply_lazy (fun () -> Runtime'.Deserialize_json.deserialize ~message_name:(name ()) (spec ()) constructor)
    let from_json json = Runtime'.Result.catch (fun () -> from_json_exn json)
  end

  and FetchResponse : sig
    type t = {
      success:bool;
      error:string;
      body:string;
      (**
{%html:
<p>Response body</p>
%}
      *)

      status_code:int;
      (**
{%html:
<p>HTTP status code</p>
%}
      *)

      attestation_id:string;
      (**
{%html:
<p>ID of created attestation</p>
%}
      *)

    }
    val make: ?success:bool -> ?error:string -> ?body:string -> ?status_code:int -> ?attestation_id:string -> unit -> t
    (** Helper function to generate a message using default values *)

    val to_proto: t -> Runtime'.Writer.t
    (** Serialize the message to binary format *)

    val from_proto: Runtime'.Reader.t -> (t, [> Runtime'.Result.error]) result
    (** Deserialize from binary format *)

    val to_json: Runtime'.Json_options.t -> t -> Runtime'.Json.t
    (** Serialize to Json (compatible with Yojson.Basic.t) *)

    val from_json: Runtime'.Json.t -> (t, [> Runtime'.Result.error]) result
    (** Deserialize from Json (compatible with Yojson.Basic.t) *)

    val name: unit -> string
    (** Fully qualified protobuf name of this message *)

    (**/**)
    type make_t = ?success:bool -> ?error:string -> ?body:string -> ?status_code:int -> ?attestation_id:string -> unit -> t
    val merge: t -> t -> t
    val to_proto': Runtime'.Writer.t -> t -> unit
    val from_proto_exn: Runtime'.Reader.t -> t
    val from_json_exn: Runtime'.Json.t -> t
    (**/**)
  end = struct
    module This'_ = FetchResponse
    let name () = ".protocol.FetchResponse"
    type t = {
      success:bool;
      error:string;
      body:string;
      status_code:int;
      attestation_id:string;
    }
    type make_t = ?success:bool -> ?error:string -> ?body:string -> ?status_code:int -> ?attestation_id:string -> unit -> t
    let make ?(success = false) ?(error = {||}) ?(body = {||}) ?(status_code = 0) ?(attestation_id = {||}) () = { success; error; body; status_code; attestation_id }
    let merge =
    let merge_success = Runtime'.Merge.merge Runtime'.Spec.( basic ((1, "success", "success"), bool, (false)) ) in
    let merge_error = Runtime'.Merge.merge Runtime'.Spec.( basic ((2, "error", "error"), string, ({||})) ) in
    let merge_body = Runtime'.Merge.merge Runtime'.Spec.( basic ((3, "body", "body"), string, ({||})) ) in
    let merge_status_code = Runtime'.Merge.merge Runtime'.Spec.( basic ((4, "status_code", "statusCode"), int32_int, (0)) ) in
    let merge_attestation_id = Runtime'.Merge.merge Runtime'.Spec.( basic ((5, "attestation_id", "attestationId"), string, ({||})) ) in
    fun t1 t2 -> {
    	success = (merge_success t1.success t2.success);
    	error = (merge_error t1.error t2.error);
    	body = (merge_body t1.body t2.body);
    	status_code = (merge_status_code t1.status_code t2.status_code);
    	attestation_id = (merge_attestation_id t1.attestation_id t2.attestation_id);
     }
    let spec () = Runtime'.Spec.( basic ((1, "success", "success"), bool, (false)) ^:: basic ((2, "error", "error"), string, ({||})) ^:: basic ((3, "body", "body"), string, ({||})) ^:: basic ((4, "status_code", "statusCode"), int32_int, (0)) ^:: basic ((5, "attestation_id", "attestationId"), string, ({||})) ^:: nil )
    let to_proto' =
      let serialize = Runtime'.apply_lazy (fun () -> Runtime'.Serialize.serialize (spec ())) in
      fun writer { success; error; body; status_code; attestation_id } -> serialize writer success error body status_code attestation_id

    let to_proto t = let writer = Runtime'.Writer.init () in to_proto' writer t; writer
    let from_proto_exn =
      let constructor success error body status_code attestation_id = { success; error; body; status_code; attestation_id } in
      Runtime'.apply_lazy (fun () -> Runtime'.Deserialize.deserialize (spec ()) constructor)
    let from_proto writer = Runtime'.Result.catch (fun () -> from_proto_exn writer)
    let to_json options =
      let serialize = Runtime'.Serialize_json.serialize ~message_name:(name ()) (spec ()) options in
      fun { success; error; body; status_code; attestation_id } -> serialize success error body status_code attestation_id
    let from_json_exn =
      let constructor success error body status_code attestation_id = { success; error; body; status_code; attestation_id } in
      Runtime'.apply_lazy (fun () -> Runtime'.Deserialize_json.deserialize ~message_name:(name ()) (spec ()) constructor)
    let from_json json = Runtime'.Result.catch (fun () -> from_json_exn json)
  end

  module FetchService = struct
    module Fetch = struct
      let package_name = Some "protocol"
      let service_name = "FetchService"
      let method_name = "Fetch"
      let name = "/protocol.FetchService/Fetch"
      module Request = FetchRequest
      module Response = FetchResponse
    end

    let fetch =
      (module FetchRequest : Runtime'.Spec.Message with type t = FetchRequest.t ),
      (module FetchResponse : Runtime'.Spec.Message with type t = FetchResponse.t )

  end

end

