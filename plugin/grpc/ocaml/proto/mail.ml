(********************************************************)
(*           AUTOGENERATED FILE - DO NOT EDIT!          *)
(********************************************************)
(* Generated by: ocaml-protoc-plugin                    *)
(* https://github.com/andersfugmann/ocaml-protoc-plugin *)
(********************************************************)
(*
  Source: plugin/grpc/protocol/mail.proto
  Syntax: proto3
  Parameters:
    debug=false
    annot=''
    opens=[]
    int64_as_int=true
    int32_as_int=true
    fixed_as_int=false
    singleton_record=false
    prefix_output_with_package=false
*)
[@@@ocaml.alert "-protobuf"] (* Disable deprecation warnings for protobuf*)
(**/**)
module Runtime' = Ocaml_protoc_plugin [@@warning "-33"]
module Imported'modules = struct
end
(**/**)
module rec Protocol : sig

  (**
{%html:
<p>MailTemplate is what a mail is filled from. Each part is a Go template over
the values a Send names: subject and text with text/template, html with
html/template. A value a template names and the Send does not is a refusal,
never an empty space in a mail that went out.</p>
%}
  *)
  module rec MailTemplate : sig
    type t = {
      subject:string;
      html:string;
      text:string;
    }
    val make: ?subject:string -> ?html:string -> ?text:string -> unit -> t
    (** Helper function to generate a message using default values *)

    val to_proto: t -> Runtime'.Writer.t
    (** Serialize the message to binary format *)

    val from_proto: Runtime'.Reader.t -> (t, [> Runtime'.Result.error]) result
    (** Deserialize from binary format *)

    val to_json: Runtime'.Json_options.t -> t -> Runtime'.Json.t
    (** Serialize to Json (compatible with Yojson.Basic.t) *)

    val from_json: Runtime'.Json.t -> (t, [> Runtime'.Result.error]) result
    (** Deserialize from Json (compatible with Yojson.Basic.t) *)

    val name: unit -> string
    (** Fully qualified protobuf name of this message *)

    (**/**)
    type make_t = ?subject:string -> ?html:string -> ?text:string -> unit -> t
    val merge: t -> t -> t
    val to_proto': Runtime'.Writer.t -> t -> unit
    val from_proto_exn: Runtime'.Reader.t -> t
    val from_json_exn: Runtime'.Json.t -> t
    (**/**)
  end

  and SetMailTemplateRequest : sig
    type t = {
      auth_token:string;
      source:string;
      (**
{%html:
<p>The plugin setting it. A plugin's templates are its own.</p>
%}
      *)

      name:string;
      (**
{%html:
<p>The template's name within the plugin.</p>
%}
      *)

      template:MailTemplate.t option;
    }
    val make: ?auth_token:string -> ?source:string -> ?name:string -> ?template:MailTemplate.t -> unit -> t
    (** Helper function to generate a message using default values *)

    val to_proto: t -> Runtime'.Writer.t
    (** Serialize the message to binary format *)

    val from_proto: Runtime'.Reader.t -> (t, [> Runtime'.Result.error]) result
    (** Deserialize from binary format *)

    val to_json: Runtime'.Json_options.t -> t -> Runtime'.Json.t
    (** Serialize to Json (compatible with Yojson.Basic.t) *)

    val from_json: Runtime'.Json.t -> (t, [> Runtime'.Result.error]) result
    (** Deserialize from Json (compatible with Yojson.Basic.t) *)

    val name: unit -> string
    (** Fully qualified protobuf name of this message *)

    (**/**)
    type make_t = ?auth_token:string -> ?source:string -> ?name:string -> ?template:MailTemplate.t -> unit -> t
    val merge: t -> t -> t
    val to_proto': Runtime'.Writer.t -> t -> unit
    val from_proto_exn: Runtime'.Reader.t -> t
    val from_json_exn: Runtime'.Json.t -> t
    (**/**)
  end

  and SetMailTemplateResponse : sig
    type t = {
      success:bool;
      error:string;
      attestation_id:string;
      (**
{%html:
<p>The attestation the template is kept as.</p>
%}
      *)

    }
    val make: ?success:bool -> ?error:string -> ?attestation_id:string -> unit -> t
    (** Helper function to generate a message using default values *)

    val to_proto: t -> Runtime'.Writer.t
    (** Serialize the message to binary format *)

    val from_proto: Runtime'.Reader.t -> (t, [> Runtime'.Result.error]) result
    (** Deserialize from binary format *)

    val to_json: Runtime'.Json_options.t -> t -> Runtime'.Json.t
    (** Serialize to Json (compatible with Yojson.Basic.t) *)

    val from_json: Runtime'.Json.t -> (t, [> Runtime'.Result.error]) result
    (** Deserialize from Json (compatible with Yojson.Basic.t) *)

    val name: unit -> string
    (** Fully qualified protobuf name of this message *)

    (**/**)
    type make_t = ?success:bool -> ?error:string -> ?attestation_id:string -> unit -> t
    val merge: t -> t -> t
    val to_proto': Runtime'.Writer.t -> t -> unit
    val from_proto_exn: Runtime'.Reader.t -> t
    val from_json_exn: Runtime'.Json.t -> t
    (**/**)
  end

  and SendMailRequest : sig
    type t = {
      auth_token:string;
      source:string;
      (**
{%html:
<p>The plugin sending.</p>
%}
      *)

      user_id:string;
      (**
{%html:
<p>The User it goes to. Their primary address is the one used.</p>
%}
      *)

      template:string;
      (**
{%html:
<p>A name the plugin set. Empty is QNTX's neutral template.</p>
%}
      *)

      values:(string * string) list;
      (**
{%html:
<p>What the template is filled with.</p>
%}
      *)

      inline:MailImage.t list;
      (**
{%html:
<p>Images the html shows by cid:&lt;content_id&gt;.</p>
%}
      *)

    }
    val make: ?auth_token:string -> ?source:string -> ?user_id:string -> ?template:string -> ?values:(string * string) list -> ?inline:MailImage.t list -> unit -> t
    (** Helper function to generate a message using default values *)

    val to_proto: t -> Runtime'.Writer.t
    (** Serialize the message to binary format *)

    val from_proto: Runtime'.Reader.t -> (t, [> Runtime'.Result.error]) result
    (** Deserialize from binary format *)

    val to_json: Runtime'.Json_options.t -> t -> Runtime'.Json.t
    (** Serialize to Json (compatible with Yojson.Basic.t) *)

    val from_json: Runtime'.Json.t -> (t, [> Runtime'.Result.error]) result
    (** Deserialize from Json (compatible with Yojson.Basic.t) *)

    val name: unit -> string
    (** Fully qualified protobuf name of this message *)

    (**/**)
    type make_t = ?auth_token:string -> ?source:string -> ?user_id:string -> ?template:string -> ?values:(string * string) list -> ?inline:MailImage.t list -> unit -> t
    val merge: t -> t -> t
    val to_proto': Runtime'.Writer.t -> t -> unit
    val from_proto_exn: Runtime'.Reader.t -> t
    val from_json_exn: Runtime'.Json.t -> t
    (**/**)
  end


  (**
{%html:
<p>MailImage is an image a mail carries inline. Only image/png is accepted, and
all of a mail's images together are held under a size cap.</p>
%}
  *)
  and MailImage : sig
    type t = {
      content_id:string;
      (**
{%html:
<p>The html shows it by cid:&lt;content_id&gt;. Unique within a mail.</p>
%}
      *)

      content_type:string;
      (**
{%html:
<p>image/png.</p>
%}
      *)

      file_name:string;
      (**
{%html:
<p>Empty is &lt;content_id&gt;.png.</p>
%}
      *)

      data:bytes;
    }
    val make: ?content_id:string -> ?content_type:string -> ?file_name:string -> ?data:bytes -> unit -> t
    (** Helper function to generate a message using default values *)

    val to_proto: t -> Runtime'.Writer.t
    (** Serialize the message to binary format *)

    val from_proto: Runtime'.Reader.t -> (t, [> Runtime'.Result.error]) result
    (** Deserialize from binary format *)

    val to_json: Runtime'.Json_options.t -> t -> Runtime'.Json.t
    (** Serialize to Json (compatible with Yojson.Basic.t) *)

    val from_json: Runtime'.Json.t -> (t, [> Runtime'.Result.error]) result
    (** Deserialize from Json (compatible with Yojson.Basic.t) *)

    val name: unit -> string
    (** Fully qualified protobuf name of this message *)

    (**/**)
    type make_t = ?content_id:string -> ?content_type:string -> ?file_name:string -> ?data:bytes -> unit -> t
    val merge: t -> t -> t
    val to_proto': Runtime'.Writer.t -> t -> unit
    val from_proto_exn: Runtime'.Reader.t -> t
    val from_json_exn: Runtime'.Json.t -> t
    (**/**)
  end

  and SendMailResponse : sig
    type t = {
      success:bool;
      error:string;
      message_id:string;
      (**
{%html:
<p>The id the transport gave the mail.</p>
%}
      *)

      attestation_id:string;
      (**
{%html:
<p>The attestation of the mail.</p>
%}
      *)

    }
    val make: ?success:bool -> ?error:string -> ?message_id:string -> ?attestation_id:string -> unit -> t
    (** Helper function to generate a message using default values *)

    val to_proto: t -> Runtime'.Writer.t
    (** Serialize the message to binary format *)

    val from_proto: Runtime'.Reader.t -> (t, [> Runtime'.Result.error]) result
    (** Deserialize from binary format *)

    val to_json: Runtime'.Json_options.t -> t -> Runtime'.Json.t
    (** Serialize to Json (compatible with Yojson.Basic.t) *)

    val from_json: Runtime'.Json.t -> (t, [> Runtime'.Result.error]) result
    (** Deserialize from Json (compatible with Yojson.Basic.t) *)

    val name: unit -> string
    (** Fully qualified protobuf name of this message *)

    (**/**)
    type make_t = ?success:bool -> ?error:string -> ?message_id:string -> ?attestation_id:string -> unit -> t
    val merge: t -> t -> t
    val to_proto': Runtime'.Writer.t -> t -> unit
    val from_proto_exn: Runtime'.Reader.t -> t
    val from_json_exn: Runtime'.Json.t -> t
    (**/**)
  end

  module MailService : sig
    module SetTemplate : sig
      include Runtime'.Service.Rpc with type Request.t = SetMailTemplateRequest.t and type Response.t = SetMailTemplateResponse.t
      module Request : Runtime'.Spec.Message with type t = SetMailTemplateRequest.t and type make_t = SetMailTemplateRequest.make_t
      (** Module alias for the request message for this method call *)

      module Response : Runtime'.Spec.Message with type t = SetMailTemplateResponse.t and type make_t = SetMailTemplateResponse.make_t
      (** Module alias for the response message for this method call *)

    end

    val setTemplate : (module Runtime'.Spec.Message with type t = SetMailTemplateRequest.t) * (module Runtime'.Spec.Message with type t = SetMailTemplateResponse.t)
    module Send : sig
      include Runtime'.Service.Rpc with type Request.t = SendMailRequest.t and type Response.t = SendMailResponse.t
      module Request : Runtime'.Spec.Message with type t = SendMailRequest.t and type make_t = SendMailRequest.make_t
      (** Module alias for the request message for this method call *)

      module Response : Runtime'.Spec.Message with type t = SendMailResponse.t and type make_t = SendMailResponse.make_t
      (** Module alias for the response message for this method call *)

    end

    val send : (module Runtime'.Spec.Message with type t = SendMailRequest.t) * (module Runtime'.Spec.Message with type t = SendMailResponse.t)
  end

end = struct
  module rec MailTemplate : sig
    type t = {
      subject:string;
      html:string;
      text:string;
    }
    val make: ?subject:string -> ?html:string -> ?text:string -> unit -> t
    (** Helper function to generate a message using default values *)

    val to_proto: t -> Runtime'.Writer.t
    (** Serialize the message to binary format *)

    val from_proto: Runtime'.Reader.t -> (t, [> Runtime'.Result.error]) result
    (** Deserialize from binary format *)

    val to_json: Runtime'.Json_options.t -> t -> Runtime'.Json.t
    (** Serialize to Json (compatible with Yojson.Basic.t) *)

    val from_json: Runtime'.Json.t -> (t, [> Runtime'.Result.error]) result
    (** Deserialize from Json (compatible with Yojson.Basic.t) *)

    val name: unit -> string
    (** Fully qualified protobuf name of this message *)

    (**/**)
    type make_t = ?subject:string -> ?html:string -> ?text:string -> unit -> t
    val merge: t -> t -> t
    val to_proto': Runtime'.Writer.t -> t -> unit
    val from_proto_exn: Runtime'.Reader.t -> t
    val from_json_exn: Runtime'.Json.t -> t
    (**/**)
  end = struct
    module This'_ = MailTemplate
    let name () = ".protocol.MailTemplate"
    type t = {
      subject:string;
      html:string;
      text:string;
    }
    type make_t = ?subject:string -> ?html:string -> ?text:string -> unit -> t
    let make ?(subject = {||}) ?(html = {||}) ?(text = {||}) () = { subject; html; text }
    let merge =
    let merge_subject = Runtime'.Merge.merge Runtime'.Spec.( basic ((1, "subject", "subject"), string, ({||})) ) in
    let merge_html = Runtime'.Merge.merge Runtime'.Spec.( basic ((2, "html", "html"), string, ({||})) ) in
    let merge_text = Runtime'.Merge.merge Runtime'.Spec.( basic ((3, "text", "text"), string, ({||})) ) in
    fun t1 t2 -> {
    	subject = (merge_subject t1.subject t2.subject);
    	html = (merge_html t1.html t2.html);
    	text = (merge_text t1.text t2.text);
     }
    let spec () = Runtime'.Spec.( basic ((1, "subject", "subject"), string, ({||})) ^:: basic ((2, "html", "html"), string, ({||})) ^:: basic ((3, "text", "text"), string, ({||})) ^:: nil )
    let to_proto' =
      let serialize = Runtime'.apply_lazy (fun () -> Runtime'.Serialize.serialize (spec ())) in
      fun writer { subject; html; text } -> serialize writer subject html text

    let to_proto t = let writer = Runtime'.Writer.init () in to_proto' writer t; writer
    let from_proto_exn =
      let constructor subject html text = { subject; html; text } in
      Runtime'.apply_lazy (fun () -> Runtime'.Deserialize.deserialize (spec ()) constructor)
    let from_proto writer = Runtime'.Result.catch (fun () -> from_proto_exn writer)
    let to_json options =
      let serialize = Runtime'.Serialize_json.serialize ~message_name:(name ()) (spec ()) options in
      fun { subject; html; text } -> serialize subject html text
    let from_json_exn =
      let constructor subject html text = { subject; html; text } in
      Runtime'.apply_lazy (fun () -> Runtime'.Deserialize_json.deserialize ~message_name:(name ()) (spec ()) constructor)
    let from_json json = Runtime'.Result.catch (fun () -> from_json_exn json)
  end

  and SetMailTemplateRequest : sig
    type t = {
      auth_token:string;
      source:string;
      (**
{%html:
<p>The plugin setting it. A plugin's templates are its own.</p>
%}
      *)

      name:string;
      (**
{%html:
<p>The template's name within the plugin.</p>
%}
      *)

      template:MailTemplate.t option;
    }
    val make: ?auth_token:string -> ?source:string -> ?name:string -> ?template:MailTemplate.t -> unit -> t
    (** Helper function to generate a message using default values *)

    val to_proto: t -> Runtime'.Writer.t
    (** Serialize the message to binary format *)

    val from_proto: Runtime'.Reader.t -> (t, [> Runtime'.Result.error]) result
    (** Deserialize from binary format *)

    val to_json: Runtime'.Json_options.t -> t -> Runtime'.Json.t
    (** Serialize to Json (compatible with Yojson.Basic.t) *)

    val from_json: Runtime'.Json.t -> (t, [> Runtime'.Result.error]) result
    (** Deserialize from Json (compatible with Yojson.Basic.t) *)

    val name: unit -> string
    (** Fully qualified protobuf name of this message *)

    (**/**)
    type make_t = ?auth_token:string -> ?source:string -> ?name:string -> ?template:MailTemplate.t -> unit -> t
    val merge: t -> t -> t
    val to_proto': Runtime'.Writer.t -> t -> unit
    val from_proto_exn: Runtime'.Reader.t -> t
    val from_json_exn: Runtime'.Json.t -> t
    (**/**)
  end = struct
    module This'_ = SetMailTemplateRequest
    let name () = ".protocol.SetMailTemplateRequest"
    type t = {
      auth_token:string;
      source:string;
      name:string;
      template:MailTemplate.t option;
    }
    type make_t = ?auth_token:string -> ?source:string -> ?name:string -> ?template:MailTemplate.t -> unit -> t
    let make ?(auth_token = {||}) ?(source = {||}) ?(name = {||}) ?template () = { auth_token; source; name; template }
    let merge =
    let merge_auth_token = Runtime'.Merge.merge Runtime'.Spec.( basic ((1, "auth_token", "authToken"), string, ({||})) ) in
    let merge_source = Runtime'.Merge.merge Runtime'.Spec.( basic ((2, "source", "source"), string, ({||})) ) in
    let merge_name = Runtime'.Merge.merge Runtime'.Spec.( basic ((3, "name", "name"), string, ({||})) ) in
    let merge_template = Runtime'.Merge.merge Runtime'.Spec.( basic_opt ((4, "template", "template"), (message (module MailTemplate))) ) in
    fun t1 t2 -> {
    	auth_token = (merge_auth_token t1.auth_token t2.auth_token);
    	source = (merge_source t1.source t2.source);
    	name = (merge_name t1.name t2.name);
    	template = (merge_template t1.template t2.template);
     }
    let spec () = Runtime'.Spec.( basic ((1, "auth_token", "authToken"), string, ({||})) ^:: basic ((2, "source", "source"), string, ({||})) ^:: basic ((3, "name", "name"), string, ({||})) ^:: basic_opt ((4, "template", "template"), (message (module MailTemplate))) ^:: nil )
    let to_proto' =
      let serialize = Runtime'.apply_lazy (fun () -> Runtime'.Serialize.serialize (spec ())) in
      fun writer { auth_token; source; name; template } -> serialize writer auth_token source name template

    let to_proto t = let writer = Runtime'.Writer.init () in to_proto' writer t; writer
    let from_proto_exn =
      let constructor auth_token source name template = { auth_token; source; name; template } in
      Runtime'.apply_lazy (fun () -> Runtime'.Deserialize.deserialize (spec ()) constructor)
    let from_proto writer = Runtime'.Result.catch (fun () -> from_proto_exn writer)
    let to_json options =
      let serialize = Runtime'.Serialize_json.serialize ~message_name:(name ()) (spec ()) options in
      fun { auth_token; source; name; template } -> serialize auth_token source name template
    let from_json_exn =
      let constructor auth_token source name template = { auth_token; source; name; template } in
      Runtime'.apply_lazy (fun () -> Runtime'.Deserialize_json.deserialize ~message_name:(name ()) (spec ()) constructor)
    let from_json json = Runtime'.Result.catch (fun () -> from_json_exn json)
  end

  and SetMailTemplateResponse : sig
    type t = {
      success:bool;
      error:string;
      attestation_id:string;
      (**
{%html:
<p>The attestation the template is kept as.</p>
%}
      *)

    }
    val make: ?success:bool -> ?error:string -> ?attestation_id:string -> unit -> t
    (** Helper function to generate a message using default values *)

    val to_proto: t -> Runtime'.Writer.t
    (** Serialize the message to binary format *)

    val from_proto: Runtime'.Reader.t -> (t, [> Runtime'.Result.error]) result
    (** Deserialize from binary format *)

    val to_json: Runtime'.Json_options.t -> t -> Runtime'.Json.t
    (** Serialize to Json (compatible with Yojson.Basic.t) *)

    val from_json: Runtime'.Json.t -> (t, [> Runtime'.Result.error]) result
    (** Deserialize from Json (compatible with Yojson.Basic.t) *)

    val name: unit -> string
    (** Fully qualified protobuf name of this message *)

    (**/**)
    type make_t = ?success:bool -> ?error:string -> ?attestation_id:string -> unit -> t
    val merge: t -> t -> t
    val to_proto': Runtime'.Writer.t -> t -> unit
    val from_proto_exn: Runtime'.Reader.t -> t
    val from_json_exn: Runtime'.Json.t -> t
    (**/**)
  end = struct
    module This'_ = SetMailTemplateResponse
    let name () = ".protocol.SetMailTemplateResponse"
    type t = {
      success:bool;
      error:string;
      attestation_id:string;
    }
    type make_t = ?success:bool -> ?error:string -> ?attestation_id:string -> unit -> t
    let make ?(success = false) ?(error = {||}) ?(attestation_id = {||}) () = { success; error; attestation_id }
    let merge =
    let merge_success = Runtime'.Merge.merge Runtime'.Spec.( basic ((1, "success", "success"), bool, (false)) ) in
    let merge_error = Runtime'.Merge.merge Runtime'.Spec.( basic ((2, "error", "error"), string, ({||})) ) in
    let merge_attestation_id = Runtime'.Merge.merge Runtime'.Spec.( basic ((3, "attestation_id", "attestationId"), string, ({||})) ) in
    fun t1 t2 -> {
    	success = (merge_success t1.success t2.success);
    	error = (merge_error t1.error t2.error);
    	attestation_id = (merge_attestation_id t1.attestation_id t2.attestation_id);
     }
    let spec () = Runtime'.Spec.( basic ((1, "success", "success"), bool, (false)) ^:: basic ((2, "error", "error"), string, ({||})) ^:: basic ((3, "attestation_id", "attestationId"), string, ({||})) ^:: nil )
    let to_proto' =
      let serialize = Runtime'.apply_lazy (fun () -> Runtime'.Serialize.serialize (spec ())) in
      fun writer { success; error; attestation_id } -> serialize writer success error attestation_id

    let to_proto t = let writer = Runtime'.Writer.init () in to_proto' writer t; writer
    let from_proto_exn =
      let constructor success error attestation_id = { success; error; attestation_id } in
      Runtime'.apply_lazy (fun () -> Runtime'.Deserialize.deserialize (spec ()) constructor)
    let from_proto writer = Runtime'.Result.catch (fun () -> from_proto_exn writer)
    let to_json options =
      let serialize = Runtime'.Serialize_json.serialize ~message_name:(name ()) (spec ()) options in
      fun { success; error; attestation_id } -> serialize success error attestation_id
    let from_json_exn =
      let constructor success error attestation_id = { success; error; attestation_id } in
      Runtime'.apply_lazy (fun () -> Runtime'.Deserialize_json.deserialize ~message_name:(name ()) (spec ()) constructor)
    let from_json json = Runtime'.Result.catch (fun () -> from_json_exn json)
  end

  and SendMailRequest : sig
    type t = {
      auth_token:string;
      source:string;
      (**
{%html:
<p>The plugin sending.</p>
%}
      *)

      user_id:string;
      (**
{%html:
<p>The User it goes to. Their primary address is the one used.</p>
%}
      *)

      template:string;
      (**
{%html:
<p>A name the plugin set. Empty is QNTX's neutral template.</p>
%}
      *)

      values:(string * string) list;
      (**
{%html:
<p>What the template is filled with.</p>
%}
      *)

      inline:MailImage.t list;
      (**
{%html:
<p>Images the html shows by cid:&lt;content_id&gt;.</p>
%}
      *)

    }
    val make: ?auth_token:string -> ?source:string -> ?user_id:string -> ?template:string -> ?values:(string * string) list -> ?inline:MailImage.t list -> unit -> t
    (** Helper function to generate a message using default values *)

    val to_proto: t -> Runtime'.Writer.t
    (** Serialize the message to binary format *)

    val from_proto: Runtime'.Reader.t -> (t, [> Runtime'.Result.error]) result
    (** Deserialize from binary format *)

    val to_json: Runtime'.Json_options.t -> t -> Runtime'.Json.t
    (** Serialize to Json (compatible with Yojson.Basic.t) *)

    val from_json: Runtime'.Json.t -> (t, [> Runtime'.Result.error]) result
    (** Deserialize from Json (compatible with Yojson.Basic.t) *)

    val name: unit -> string
    (** Fully qualified protobuf name of this message *)

    (**/**)
    type make_t = ?auth_token:string -> ?source:string -> ?user_id:string -> ?template:string -> ?values:(string * string) list -> ?inline:MailImage.t list -> unit -> t
    val merge: t -> t -> t
    val to_proto': Runtime'.Writer.t -> t -> unit
    val from_proto_exn: Runtime'.Reader.t -> t
    val from_json_exn: Runtime'.Json.t -> t
    (**/**)
  end = struct
    module This'_ = SendMailRequest
    let name () = ".protocol.SendMailRequest"
    type t = {
      auth_token:string;
      source:string;
      user_id:string;
      template:string;
      values:(string * string) list;
      inline:MailImage.t list;
    }
    type make_t = ?auth_token:string -> ?source:string -> ?user_id:string -> ?template:string -> ?values:(string * string) list -> ?inline:MailImage.t list -> unit -> t
    let make ?(auth_token = {||}) ?(source = {||}) ?(user_id = {||}) ?(template = {||}) ?(values = []) ?(inline = []) () = { auth_token; source; user_id; template; values; inline }
    let merge =
    let merge_auth_token = Runtime'.Merge.merge Runtime'.Spec.( basic ((1, "auth_token", "authToken"), string, ({||})) ) in
    let merge_source = Runtime'.Merge.merge Runtime'.Spec.( basic ((2, "source", "source"), string, ({||})) ) in
    let merge_user_id = Runtime'.Merge.merge Runtime'.Spec.( basic ((3, "user_id", "userId"), string, ({||})) ) in
    let merge_template = Runtime'.Merge.merge Runtime'.Spec.( basic ((4, "template", "template"), string, ({||})) ) in
    let merge_values = Runtime'.Merge.merge Runtime'.Spec.( map ((5, "values", "values"), (string, basic ((2, "value", "value"), string, ({||})))) ) in
    let merge_inline = Runtime'.Merge.merge Runtime'.Spec.( repeated ((6, "inline", "inline"), (message (module MailImage)), not_packed) ) in
    fun t1 t2 -> {
    	auth_token = (merge_auth_token t1.auth_token t2.auth_token);
    	source = (merge_source t1.source t2.source);
    	user_id = (merge_user_id t1.user_id t2.user_id);
    	template = (merge_template t1.template t2.template);
    	values = (merge_values t1.values t2.values);
    	inline = (merge_inline t1.inline t2.inline);
     }
    let spec () = Runtime'.Spec.( basic ((1, "auth_token", "authToken"), string, ({||})) ^:: basic ((2, "source", "source"), string, ({||})) ^:: basic ((3, "user_id", "userId"), string, ({||})) ^:: basic ((4, "template", "template"), string, ({||})) ^:: map ((5, "values", "values"), (string, basic ((2, "value", "value"), string, ({||})))) ^:: repeated ((6, "inline", "inline"), (message (module MailImage)), not_packed) ^:: nil )
    let to_proto' =
      let serialize = Runtime'.apply_lazy (fun () -> Runtime'.Serialize.serialize (spec ())) in
      fun writer { auth_token; source; user_id; template; values; inline } -> serialize writer auth_token source user_id template values inline

    let to_proto t = let writer = Runtime'.Writer.init () in to_proto' writer t; writer
    let from_proto_exn =
      let constructor auth_token source user_id template values inline = { auth_token; source; user_id; template; values; inline } in
      Runtime'.apply_lazy (fun () -> Runtime'.Deserialize.deserialize (spec ()) constructor)
    let from_proto writer = Runtime'.Result.catch (fun () -> from_proto_exn writer)
    let to_json options =
      let serialize = Runtime'.Serialize_json.serialize ~message_name:(name ()) (spec ()) options in
      fun { auth_token; source; user_id; template; values; inline } -> serialize auth_token source user_id template values inline
    let from_json_exn =
      let constructor auth_token source user_id template values inline = { auth_token; source; user_id; template; values; inline } in
      Runtime'.apply_lazy (fun () -> Runtime'.Deserialize_json.deserialize ~message_name:(name ()) (spec ()) constructor)
    let from_json json = Runtime'.Result.catch (fun () -> from_json_exn json)
  end

  and MailImage : sig
    type t = {
      content_id:string;
      (**
{%html:
<p>The html shows it by cid:&lt;content_id&gt;. Unique within a mail.</p>
%}
      *)

      content_type:string;
      (**
{%html:
<p>image/png.</p>
%}
      *)

      file_name:string;
      (**
{%html:
<p>Empty is &lt;content_id&gt;.png.</p>
%}
      *)

      data:bytes;
    }
    val make: ?content_id:string -> ?content_type:string -> ?file_name:string -> ?data:bytes -> unit -> t
    (** Helper function to generate a message using default values *)

    val to_proto: t -> Runtime'.Writer.t
    (** Serialize the message to binary format *)

    val from_proto: Runtime'.Reader.t -> (t, [> Runtime'.Result.error]) result
    (** Deserialize from binary format *)

    val to_json: Runtime'.Json_options.t -> t -> Runtime'.Json.t
    (** Serialize to Json (compatible with Yojson.Basic.t) *)

    val from_json: Runtime'.Json.t -> (t, [> Runtime'.Result.error]) result
    (** Deserialize from Json (compatible with Yojson.Basic.t) *)

    val name: unit -> string
    (** Fully qualified protobuf name of this message *)

    (**/**)
    type make_t = ?content_id:string -> ?content_type:string -> ?file_name:string -> ?data:bytes -> unit -> t
    val merge: t -> t -> t
    val to_proto': Runtime'.Writer.t -> t -> unit
    val from_proto_exn: Runtime'.Reader.t -> t
    val from_json_exn: Runtime'.Json.t -> t
    (**/**)
  end = struct
    module This'_ = MailImage
    let name () = ".protocol.MailImage"
    type t = {
      content_id:string;
      content_type:string;
      file_name:string;
      data:bytes;
    }
    type make_t = ?content_id:string -> ?content_type:string -> ?file_name:string -> ?data:bytes -> unit -> t
    let make ?(content_id = {||}) ?(content_type = {||}) ?(file_name = {||}) ?(data = (Bytes.of_string {||})) () = { content_id; content_type; file_name; data }
    let merge =
    let merge_content_id = Runtime'.Merge.merge Runtime'.Spec.( basic ((1, "content_id", "contentId"), string, ({||})) ) in
    let merge_content_type = Runtime'.Merge.merge Runtime'.Spec.( basic ((2, "content_type", "contentType"), string, ({||})) ) in
    let merge_file_name = Runtime'.Merge.merge Runtime'.Spec.( basic ((3, "file_name", "fileName"), string, ({||})) ) in
    let merge_data = Runtime'.Merge.merge Runtime'.Spec.( basic ((4, "data", "data"), bytes, ((Bytes.of_string {||}))) ) in
    fun t1 t2 -> {
    	content_id = (merge_content_id t1.content_id t2.content_id);
    	content_type = (merge_content_type t1.content_type t2.content_type);
    	file_name = (merge_file_name t1.file_name t2.file_name);
    	data = (merge_data t1.data t2.data);
     }
    let spec () = Runtime'.Spec.( basic ((1, "content_id", "contentId"), string, ({||})) ^:: basic ((2, "content_type", "contentType"), string, ({||})) ^:: basic ((3, "file_name", "fileName"), string, ({||})) ^:: basic ((4, "data", "data"), bytes, ((Bytes.of_string {||}))) ^:: nil )
    let to_proto' =
      let serialize = Runtime'.apply_lazy (fun () -> Runtime'.Serialize.serialize (spec ())) in
      fun writer { content_id; content_type; file_name; data } -> serialize writer content_id content_type file_name data

    let to_proto t = let writer = Runtime'.Writer.init () in to_proto' writer t; writer
    let from_proto_exn =
      let constructor content_id content_type file_name data = { content_id; content_type; file_name; data } in
      Runtime'.apply_lazy (fun () -> Runtime'.Deserialize.deserialize (spec ()) constructor)
    let from_proto writer = Runtime'.Result.catch (fun () -> from_proto_exn writer)
    let to_json options =
      let serialize = Runtime'.Serialize_json.serialize ~message_name:(name ()) (spec ()) options in
      fun { content_id; content_type; file_name; data } -> serialize content_id content_type file_name data
    let from_json_exn =
      let constructor content_id content_type file_name data = { content_id; content_type; file_name; data } in
      Runtime'.apply_lazy (fun () -> Runtime'.Deserialize_json.deserialize ~message_name:(name ()) (spec ()) constructor)
    let from_json json = Runtime'.Result.catch (fun () -> from_json_exn json)
  end

  and SendMailResponse : sig
    type t = {
      success:bool;
      error:string;
      message_id:string;
      (**
{%html:
<p>The id the transport gave the mail.</p>
%}
      *)

      attestation_id:string;
      (**
{%html:
<p>The attestation of the mail.</p>
%}
      *)

    }
    val make: ?success:bool -> ?error:string -> ?message_id:string -> ?attestation_id:string -> unit -> t
    (** Helper function to generate a message using default values *)

    val to_proto: t -> Runtime'.Writer.t
    (** Serialize the message to binary format *)

    val from_proto: Runtime'.Reader.t -> (t, [> Runtime'.Result.error]) result
    (** Deserialize from binary format *)

    val to_json: Runtime'.Json_options.t -> t -> Runtime'.Json.t
    (** Serialize to Json (compatible with Yojson.Basic.t) *)

    val from_json: Runtime'.Json.t -> (t, [> Runtime'.Result.error]) result
    (** Deserialize from Json (compatible with Yojson.Basic.t) *)

    val name: unit -> string
    (** Fully qualified protobuf name of this message *)

    (**/**)
    type make_t = ?success:bool -> ?error:string -> ?message_id:string -> ?attestation_id:string -> unit -> t
    val merge: t -> t -> t
    val to_proto': Runtime'.Writer.t -> t -> unit
    val from_proto_exn: Runtime'.Reader.t -> t
    val from_json_exn: Runtime'.Json.t -> t
    (**/**)
  end = struct
    module This'_ = SendMailResponse
    let name () = ".protocol.SendMailResponse"
    type t = {
      success:bool;
      error:string;
      message_id:string;
      attestation_id:string;
    }
    type make_t = ?success:bool -> ?error:string -> ?message_id:string -> ?attestation_id:string -> unit -> t
    let make ?(success = false) ?(error = {||}) ?(message_id = {||}) ?(attestation_id = {||}) () = { success; error; message_id; attestation_id }
    let merge =
    let merge_success = Runtime'.Merge.merge Runtime'.Spec.( basic ((1, "success", "success"), bool, (false)) ) in
    let merge_error = Runtime'.Merge.merge Runtime'.Spec.( basic ((2, "error", "error"), string, ({||})) ) in
    let merge_message_id = Runtime'.Merge.merge Runtime'.Spec.( basic ((3, "message_id", "messageId"), string, ({||})) ) in
    let merge_attestation_id = Runtime'.Merge.merge Runtime'.Spec.( basic ((4, "attestation_id", "attestationId"), string, ({||})) ) in
    fun t1 t2 -> {
    	success = (merge_success t1.success t2.success);
    	error = (merge_error t1.error t2.error);
    	message_id = (merge_message_id t1.message_id t2.message_id);
    	attestation_id = (merge_attestation_id t1.attestation_id t2.attestation_id);
     }
    let spec () = Runtime'.Spec.( basic ((1, "success", "success"), bool, (false)) ^:: basic ((2, "error", "error"), string, ({||})) ^:: basic ((3, "message_id", "messageId"), string, ({||})) ^:: basic ((4, "attestation_id", "attestationId"), string, ({||})) ^:: nil )
    let to_proto' =
      let serialize = Runtime'.apply_lazy (fun () -> Runtime'.Serialize.serialize (spec ())) in
      fun writer { success; error; message_id; attestation_id } -> serialize writer success error message_id attestation_id

    let to_proto t = let writer = Runtime'.Writer.init () in to_proto' writer t; writer
    let from_proto_exn =
      let constructor success error message_id attestation_id = { success; error; message_id; attestation_id } in
      Runtime'.apply_lazy (fun () -> Runtime'.Deserialize.deserialize (spec ()) constructor)
    let from_proto writer = Runtime'.Result.catch (fun () -> from_proto_exn writer)
    let to_json options =
      let serialize = Runtime'.Serialize_json.serialize ~message_name:(name ()) (spec ()) options in
      fun { success; error; message_id; attestation_id } -> serialize success error message_id attestation_id
    let from_json_exn =
      let constructor success error message_id attestation_id = { success; error; message_id; attestation_id } in
      Runtime'.apply_lazy (fun () -> Runtime'.Deserialize_json.deserialize ~message_name:(name ()) (spec ()) constructor)
    let from_json json = Runtime'.Result.catch (fun () -> from_json_exn json)
  end

  module MailService = struct
    module SetTemplate = struct
      let package_name = Some "protocol"
      let service_name = "MailService"
      let method_name = "SetTemplate"
      let name = "/protocol.MailService/SetTemplate"
      module Request = SetMailTemplateRequest
      module Response = SetMailTemplateResponse
    end

    let setTemplate =
      (module SetMailTemplateRequest : Runtime'.Spec.Message with type t = SetMailTemplateRequest.t ),
      (module SetMailTemplateResponse : Runtime'.Spec.Message with type t = SetMailTemplateResponse.t )

    module Send = struct
      let package_name = Some "protocol"
      let service_name = "MailService"
      let method_name = "Send"
      let name = "/protocol.MailService/Send"
      module Request = SendMailRequest
      module Response = SendMailResponse
    end

    let send =
      (module SendMailRequest : Runtime'.Spec.Message with type t = SendMailRequest.t ),
      (module SendMailResponse : Runtime'.Spec.Message with type t = SendMailResponse.t )

  end

end

