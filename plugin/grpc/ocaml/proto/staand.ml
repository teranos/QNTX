(********************************************************)
(*           AUTOGENERATED FILE - DO NOT EDIT!          *)
(********************************************************)
(* Generated by: ocaml-protoc-plugin                    *)
(* https://github.com/andersfugmann/ocaml-protoc-plugin *)
(********************************************************)
(*
  Source: plugin/grpc/protocol/staand.proto
  Syntax: proto3
  Parameters:
    debug=false
    annot=''
    opens=[]
    int64_as_int=true
    int32_as_int=true
    fixed_as_int=false
    singleton_record=false
    prefix_output_with_package=false
*)
[@@@ocaml.alert "-protobuf"] (* Disable deprecation warnings for protobuf*)
(**/**)
module Runtime' = Ocaml_protoc_plugin [@@warning "-33"]
module Imported'modules = struct
end
(**/**)
module rec Protocol : sig

  (**
{%html:
<p>What a stand records when somebody arrives (ADR-035). The shape is the
industry's: Plausible's <code>events_v2</code> and Umami's <code>website_event</code> agree on
nearly all of it, and site analytics is finished work.</p>
%}
  *)
  module rec Arrival : sig
    type t = {
      at:string;
      (**
{%html:
<p>When the stand fired. The only field the node authors, because it is the
only one the visitor cannot claim.</p>
%}
      *)

      market:string;
      (**
{%html:
<p>The stand. Every other tool calls this site_id.</p>
%}
      *)

      slug:string;
      visitor:string;
      (**
{%html:
<p>A person, durable across visits.</p>
%}
      *)

      visit:string;
      (**
{%html:
<p>One sitting, ending when the tab does. Umami carries both ids; without
this one a person is a single visit forever.</p>
%}
      *)

      path:string;
      (**
{%html:
<p>The page. Path is what gets grouped, query is read once.</p>
%}
      *)

      query:string;
      event:string;
      (**
{%html:
<p>The event the pixel side named. Naming none is a page view, and page_view
is Google's word for it — borrowed, so a site firing gtag needs no
translation, rather than a word the node invented.</p>
%}
      *)

      referrer_domain:string;
      (**
{%html:
<p>Split on arrival. Grouping by domain is the question people ask, and a
whole URL answers it only after being taken apart again every time.</p>
%}
      *)

      referrer_path:string;
      utm_source:string;
      (**
{%html:
<p>The campaign five. Fields rather than params because every tool and every
ad platform already agrees on these names.</p>
%}
      *)

      utm_medium:string;
      utm_campaign:string;
      utm_content:string;
      utm_term:string;
      browser:string;
      (**
{%html:
<p>Derived at ingest from a user agent the node then drops. Nobody stores the
raw string: it identifies, it is unbounded, and this is what gets queried.</p>
%}
      *)

      browser_version:string;
      operating_system:string;
      operating_system_version:string;
      device:string;
      screen:string;
      (**
{%html:
<p>Sent by the snippet, because a server cannot see it. A bucket rather than
a pixel count, by convention.</p>
%}
      *)

      country:string;
      (**
{%html:
<p>Derived at ingest from an address the node then drops. The answer is what
gets asked for; the source of it is what identifies a person.</p>
%}
      *)

      region:string;
      city:string;
      language:string;
      (**
{%html:
<p>The language the browser asked in.</p>
%}
      *)

      params:(string * string) list;
      (**
{%html:
<p>What the site attached, verbatim. The node never learns what a key means —
the line between analytics for anyone and analytics for one customer.</p>
%}
      *)

      bot:bool;
      (**
{%html:
<p>What the node decided rather than read. A hit from a bot still happened;
only the judgement is ours.</p>
%}
      *)

    }
    val make: ?at:string -> ?market:string -> ?slug:string -> ?visitor:string -> ?visit:string -> ?path:string -> ?query:string -> ?event:string -> ?referrer_domain:string -> ?referrer_path:string -> ?utm_source:string -> ?utm_medium:string -> ?utm_campaign:string -> ?utm_content:string -> ?utm_term:string -> ?browser:string -> ?browser_version:string -> ?operating_system:string -> ?operating_system_version:string -> ?device:string -> ?screen:string -> ?country:string -> ?region:string -> ?city:string -> ?language:string -> ?params:(string * string) list -> ?bot:bool -> unit -> t
    (** Helper function to generate a message using default values *)

    val to_proto: t -> Runtime'.Writer.t
    (** Serialize the message to binary format *)

    val from_proto: Runtime'.Reader.t -> (t, [> Runtime'.Result.error]) result
    (** Deserialize from binary format *)

    val to_json: Runtime'.Json_options.t -> t -> Runtime'.Json.t
    (** Serialize to Json (compatible with Yojson.Basic.t) *)

    val from_json: Runtime'.Json.t -> (t, [> Runtime'.Result.error]) result
    (** Deserialize from Json (compatible with Yojson.Basic.t) *)

    val name: unit -> string
    (** Fully qualified protobuf name of this message *)

    (**/**)
    type make_t = ?at:string -> ?market:string -> ?slug:string -> ?visitor:string -> ?visit:string -> ?path:string -> ?query:string -> ?event:string -> ?referrer_domain:string -> ?referrer_path:string -> ?utm_source:string -> ?utm_medium:string -> ?utm_campaign:string -> ?utm_content:string -> ?utm_term:string -> ?browser:string -> ?browser_version:string -> ?operating_system:string -> ?operating_system_version:string -> ?device:string -> ?screen:string -> ?country:string -> ?region:string -> ?city:string -> ?language:string -> ?params:(string * string) list -> ?bot:bool -> unit -> t
    val merge: t -> t -> t
    val to_proto': Runtime'.Writer.t -> t -> unit
    val from_proto_exn: Runtime'.Reader.t -> t
    val from_json_exn: Runtime'.Json.t -> t
    (**/**)
  end


  (**
{%html:
<p>One sitting, derived from the arrivals carrying its visit id. Nothing writes
a Visit — it is a projection and the arrivals stay the record, the same way
Plausible maintains sessions as a view over its event table.</p>
%}
  *)
  and Visit : sig
    type t = {
      visit:string;
      visitor:string;
      market:string;
      slug:string;
      started:string;
      (**
{%html:
<p>Duration is the difference, carried because it is asked for constantly and
derived identically by everyone.</p>
%}
      *)

      ended:string;
      duration_seconds:int;
      entry_path:string;
      (**
{%html:
<p>The two pages worth naming out of however many they saw.</p>
%}
      *)

      exit_path:string;
      views:int;
      events:int;
      bounce:bool;
      (**
{%html:
<p>One arrival and gone. A convention, not a measurement: defining it
differently makes the number incomparable to everyone else's.</p>
%}
      *)

    }
    val make: ?visit:string -> ?visitor:string -> ?market:string -> ?slug:string -> ?started:string -> ?ended:string -> ?duration_seconds:int -> ?entry_path:string -> ?exit_path:string -> ?views:int -> ?events:int -> ?bounce:bool -> unit -> t
    (** Helper function to generate a message using default values *)

    val to_proto: t -> Runtime'.Writer.t
    (** Serialize the message to binary format *)

    val from_proto: Runtime'.Reader.t -> (t, [> Runtime'.Result.error]) result
    (** Deserialize from binary format *)

    val to_json: Runtime'.Json_options.t -> t -> Runtime'.Json.t
    (** Serialize to Json (compatible with Yojson.Basic.t) *)

    val from_json: Runtime'.Json.t -> (t, [> Runtime'.Result.error]) result
    (** Deserialize from Json (compatible with Yojson.Basic.t) *)

    val name: unit -> string
    (** Fully qualified protobuf name of this message *)

    (**/**)
    type make_t = ?visit:string -> ?visitor:string -> ?market:string -> ?slug:string -> ?started:string -> ?ended:string -> ?duration_seconds:int -> ?entry_path:string -> ?exit_path:string -> ?views:int -> ?events:int -> ?bounce:bool -> unit -> t
    val merge: t -> t -> t
    val to_proto': Runtime'.Writer.t -> t -> unit
    val from_proto_exn: Runtime'.Reader.t -> t
    val from_json_exn: Runtime'.Json.t -> t
    (**/**)
  end

end = struct
  module rec Arrival : sig
    type t = {
      at:string;
      (**
{%html:
<p>When the stand fired. The only field the node authors, because it is the
only one the visitor cannot claim.</p>
%}
      *)

      market:string;
      (**
{%html:
<p>The stand. Every other tool calls this site_id.</p>
%}
      *)

      slug:string;
      visitor:string;
      (**
{%html:
<p>A person, durable across visits.</p>
%}
      *)

      visit:string;
      (**
{%html:
<p>One sitting, ending when the tab does. Umami carries both ids; without
this one a person is a single visit forever.</p>
%}
      *)

      path:string;
      (**
{%html:
<p>The page. Path is what gets grouped, query is read once.</p>
%}
      *)

      query:string;
      event:string;
      (**
{%html:
<p>The event the pixel side named. Naming none is a page view, and page_view
is Google's word for it — borrowed, so a site firing gtag needs no
translation, rather than a word the node invented.</p>
%}
      *)

      referrer_domain:string;
      (**
{%html:
<p>Split on arrival. Grouping by domain is the question people ask, and a
whole URL answers it only after being taken apart again every time.</p>
%}
      *)

      referrer_path:string;
      utm_source:string;
      (**
{%html:
<p>The campaign five. Fields rather than params because every tool and every
ad platform already agrees on these names.</p>
%}
      *)

      utm_medium:string;
      utm_campaign:string;
      utm_content:string;
      utm_term:string;
      browser:string;
      (**
{%html:
<p>Derived at ingest from a user agent the node then drops. Nobody stores the
raw string: it identifies, it is unbounded, and this is what gets queried.</p>
%}
      *)

      browser_version:string;
      operating_system:string;
      operating_system_version:string;
      device:string;
      screen:string;
      (**
{%html:
<p>Sent by the snippet, because a server cannot see it. A bucket rather than
a pixel count, by convention.</p>
%}
      *)

      country:string;
      (**
{%html:
<p>Derived at ingest from an address the node then drops. The answer is what
gets asked for; the source of it is what identifies a person.</p>
%}
      *)

      region:string;
      city:string;
      language:string;
      (**
{%html:
<p>The language the browser asked in.</p>
%}
      *)

      params:(string * string) list;
      (**
{%html:
<p>What the site attached, verbatim. The node never learns what a key means —
the line between analytics for anyone and analytics for one customer.</p>
%}
      *)

      bot:bool;
      (**
{%html:
<p>What the node decided rather than read. A hit from a bot still happened;
only the judgement is ours.</p>
%}
      *)

    }
    val make: ?at:string -> ?market:string -> ?slug:string -> ?visitor:string -> ?visit:string -> ?path:string -> ?query:string -> ?event:string -> ?referrer_domain:string -> ?referrer_path:string -> ?utm_source:string -> ?utm_medium:string -> ?utm_campaign:string -> ?utm_content:string -> ?utm_term:string -> ?browser:string -> ?browser_version:string -> ?operating_system:string -> ?operating_system_version:string -> ?device:string -> ?screen:string -> ?country:string -> ?region:string -> ?city:string -> ?language:string -> ?params:(string * string) list -> ?bot:bool -> unit -> t
    (** Helper function to generate a message using default values *)

    val to_proto: t -> Runtime'.Writer.t
    (** Serialize the message to binary format *)

    val from_proto: Runtime'.Reader.t -> (t, [> Runtime'.Result.error]) result
    (** Deserialize from binary format *)

    val to_json: Runtime'.Json_options.t -> t -> Runtime'.Json.t
    (** Serialize to Json (compatible with Yojson.Basic.t) *)

    val from_json: Runtime'.Json.t -> (t, [> Runtime'.Result.error]) result
    (** Deserialize from Json (compatible with Yojson.Basic.t) *)

    val name: unit -> string
    (** Fully qualified protobuf name of this message *)

    (**/**)
    type make_t = ?at:string -> ?market:string -> ?slug:string -> ?visitor:string -> ?visit:string -> ?path:string -> ?query:string -> ?event:string -> ?referrer_domain:string -> ?referrer_path:string -> ?utm_source:string -> ?utm_medium:string -> ?utm_campaign:string -> ?utm_content:string -> ?utm_term:string -> ?browser:string -> ?browser_version:string -> ?operating_system:string -> ?operating_system_version:string -> ?device:string -> ?screen:string -> ?country:string -> ?region:string -> ?city:string -> ?language:string -> ?params:(string * string) list -> ?bot:bool -> unit -> t
    val merge: t -> t -> t
    val to_proto': Runtime'.Writer.t -> t -> unit
    val from_proto_exn: Runtime'.Reader.t -> t
    val from_json_exn: Runtime'.Json.t -> t
    (**/**)
  end = struct
    module This'_ = Arrival
    let name () = ".protocol.Arrival"
    type t = {
      at:string;
      market:string;
      slug:string;
      visitor:string;
      visit:string;
      path:string;
      query:string;
      event:string;
      referrer_domain:string;
      referrer_path:string;
      utm_source:string;
      utm_medium:string;
      utm_campaign:string;
      utm_content:string;
      utm_term:string;
      browser:string;
      browser_version:string;
      operating_system:string;
      operating_system_version:string;
      device:string;
      screen:string;
      country:string;
      region:string;
      city:string;
      language:string;
      params:(string * string) list;
      bot:bool;
    }
    type make_t = ?at:string -> ?market:string -> ?slug:string -> ?visitor:string -> ?visit:string -> ?path:string -> ?query:string -> ?event:string -> ?referrer_domain:string -> ?referrer_path:string -> ?utm_source:string -> ?utm_medium:string -> ?utm_campaign:string -> ?utm_content:string -> ?utm_term:string -> ?browser:string -> ?browser_version:string -> ?operating_system:string -> ?operating_system_version:string -> ?device:string -> ?screen:string -> ?country:string -> ?region:string -> ?city:string -> ?language:string -> ?params:(string * string) list -> ?bot:bool -> unit -> t
    let make ?(at = {||}) ?(market = {||}) ?(slug = {||}) ?(visitor = {||}) ?(visit = {||}) ?(path = {||}) ?(query = {||}) ?(event = {||}) ?(referrer_domain = {||}) ?(referrer_path = {||}) ?(utm_source = {||}) ?(utm_medium = {||}) ?(utm_campaign = {||}) ?(utm_content = {||}) ?(utm_term = {||}) ?(browser = {||}) ?(browser_version = {||}) ?(operating_system = {||}) ?(operating_system_version = {||}) ?(device = {||}) ?(screen = {||}) ?(country = {||}) ?(region = {||}) ?(city = {||}) ?(language = {||}) ?(params = []) ?(bot = false) () = { at; market; slug; visitor; visit; path; query; event; referrer_domain; referrer_path; utm_source; utm_medium; utm_campaign; utm_content; utm_term; browser; browser_version; operating_system; operating_system_version; device; screen; country; region; city; language; params; bot }
    let merge =
    let merge_at = Runtime'.Merge.merge Runtime'.Spec.( basic ((1, "at", "at"), string, ({||})) ) in
    let merge_market = Runtime'.Merge.merge Runtime'.Spec.( basic ((2, "market", "market"), string, ({||})) ) in
    let merge_slug = Runtime'.Merge.merge Runtime'.Spec.( basic ((3, "slug", "slug"), string, ({||})) ) in
    let merge_visitor = Runtime'.Merge.merge Runtime'.Spec.( basic ((4, "visitor", "visitor"), string, ({||})) ) in
    let merge_visit = Runtime'.Merge.merge Runtime'.Spec.( basic ((5, "visit", "visit"), string, ({||})) ) in
    let merge_path = Runtime'.Merge.merge Runtime'.Spec.( basic ((6, "path", "path"), string, ({||})) ) in
    let merge_query = Runtime'.Merge.merge Runtime'.Spec.( basic ((7, "query", "query"), string, ({||})) ) in
    let merge_event = Runtime'.Merge.merge Runtime'.Spec.( basic ((8, "event", "event"), string, ({||})) ) in
    let merge_referrer_domain = Runtime'.Merge.merge Runtime'.Spec.( basic ((9, "referrer_domain", "referrerDomain"), string, ({||})) ) in
    let merge_referrer_path = Runtime'.Merge.merge Runtime'.Spec.( basic ((10, "referrer_path", "referrerPath"), string, ({||})) ) in
    let merge_utm_source = Runtime'.Merge.merge Runtime'.Spec.( basic ((11, "utm_source", "utmSource"), string, ({||})) ) in
    let merge_utm_medium = Runtime'.Merge.merge Runtime'.Spec.( basic ((12, "utm_medium", "utmMedium"), string, ({||})) ) in
    let merge_utm_campaign = Runtime'.Merge.merge Runtime'.Spec.( basic ((13, "utm_campaign", "utmCampaign"), string, ({||})) ) in
    let merge_utm_content = Runtime'.Merge.merge Runtime'.Spec.( basic ((14, "utm_content", "utmContent"), string, ({||})) ) in
    let merge_utm_term = Runtime'.Merge.merge Runtime'.Spec.( basic ((15, "utm_term", "utmTerm"), string, ({||})) ) in
    let merge_browser = Runtime'.Merge.merge Runtime'.Spec.( basic ((16, "browser", "browser"), string, ({||})) ) in
    let merge_browser_version = Runtime'.Merge.merge Runtime'.Spec.( basic ((17, "browser_version", "browserVersion"), string, ({||})) ) in
    let merge_operating_system = Runtime'.Merge.merge Runtime'.Spec.( basic ((18, "operating_system", "operatingSystem"), string, ({||})) ) in
    let merge_operating_system_version = Runtime'.Merge.merge Runtime'.Spec.( basic ((19, "operating_system_version", "operatingSystemVersion"), string, ({||})) ) in
    let merge_device = Runtime'.Merge.merge Runtime'.Spec.( basic ((20, "device", "device"), string, ({||})) ) in
    let merge_screen = Runtime'.Merge.merge Runtime'.Spec.( basic ((21, "screen", "screen"), string, ({||})) ) in
    let merge_country = Runtime'.Merge.merge Runtime'.Spec.( basic ((22, "country", "country"), string, ({||})) ) in
    let merge_region = Runtime'.Merge.merge Runtime'.Spec.( basic ((23, "region", "region"), string, ({||})) ) in
    let merge_city = Runtime'.Merge.merge Runtime'.Spec.( basic ((24, "city", "city"), string, ({||})) ) in
    let merge_language = Runtime'.Merge.merge Runtime'.Spec.( basic ((25, "language", "language"), string, ({||})) ) in
    let merge_params = Runtime'.Merge.merge Runtime'.Spec.( map ((26, "params", "params"), (string, basic ((2, "value", "value"), string, ({||})))) ) in
    let merge_bot = Runtime'.Merge.merge Runtime'.Spec.( basic ((27, "bot", "bot"), bool, (false)) ) in
    fun t1 t2 -> {
    	at = (merge_at t1.at t2.at);
    	market = (merge_market t1.market t2.market);
    	slug = (merge_slug t1.slug t2.slug);
    	visitor = (merge_visitor t1.visitor t2.visitor);
    	visit = (merge_visit t1.visit t2.visit);
    	path = (merge_path t1.path t2.path);
    	query = (merge_query t1.query t2.query);
    	event = (merge_event t1.event t2.event);
    	referrer_domain = (merge_referrer_domain t1.referrer_domain t2.referrer_domain);
    	referrer_path = (merge_referrer_path t1.referrer_path t2.referrer_path);
    	utm_source = (merge_utm_source t1.utm_source t2.utm_source);
    	utm_medium = (merge_utm_medium t1.utm_medium t2.utm_medium);
    	utm_campaign = (merge_utm_campaign t1.utm_campaign t2.utm_campaign);
    	utm_content = (merge_utm_content t1.utm_content t2.utm_content);
    	utm_term = (merge_utm_term t1.utm_term t2.utm_term);
    	browser = (merge_browser t1.browser t2.browser);
    	browser_version = (merge_browser_version t1.browser_version t2.browser_version);
    	operating_system = (merge_operating_system t1.operating_system t2.operating_system);
    	operating_system_version = (merge_operating_system_version t1.operating_system_version t2.operating_system_version);
    	device = (merge_device t1.device t2.device);
    	screen = (merge_screen t1.screen t2.screen);
    	country = (merge_country t1.country t2.country);
    	region = (merge_region t1.region t2.region);
    	city = (merge_city t1.city t2.city);
    	language = (merge_language t1.language t2.language);
    	params = (merge_params t1.params t2.params);
    	bot = (merge_bot t1.bot t2.bot);
     }
    let spec () = Runtime'.Spec.( basic ((1, "at", "at"), string, ({||})) ^:: basic ((2, "market", "market"), string, ({||})) ^:: basic ((3, "slug", "slug"), string, ({||})) ^:: basic ((4, "visitor", "visitor"), string, ({||})) ^:: basic ((5, "visit", "visit"), string, ({||})) ^:: basic ((6, "path", "path"), string, ({||})) ^:: basic ((7, "query", "query"), string, ({||})) ^:: basic ((8, "event", "event"), string, ({||})) ^:: basic ((9, "referrer_domain", "referrerDomain"), string, ({||})) ^:: basic ((10, "referrer_path", "referrerPath"), string, ({||})) ^:: basic ((11, "utm_source", "utmSource"), string, ({||})) ^:: basic ((12, "utm_medium", "utmMedium"), string, ({||})) ^:: basic ((13, "utm_campaign", "utmCampaign"), string, ({||})) ^:: basic ((14, "utm_content", "utmContent"), string, ({||})) ^:: basic ((15, "utm_term", "utmTerm"), string, ({||})) ^:: basic ((16, "browser", "browser"), string, ({||})) ^:: basic ((17, "browser_version", "browserVersion"), string, ({||})) ^:: basic ((18, "operating_system", "operatingSystem"), string, ({||})) ^:: basic ((19, "operating_system_version", "operatingSystemVersion"), string, ({||})) ^:: basic ((20, "device", "device"), string, ({||})) ^:: basic ((21, "screen", "screen"), string, ({||})) ^:: basic ((22, "country", "country"), string, ({||})) ^:: basic ((23, "region", "region"), string, ({||})) ^:: basic ((24, "city", "city"), string, ({||})) ^:: basic ((25, "language", "language"), string, ({||})) ^:: map ((26, "params", "params"), (string, basic ((2, "value", "value"), string, ({||})))) ^:: basic ((27, "bot", "bot"), bool, (false)) ^:: nil )
    let to_proto' =
      let serialize = Runtime'.apply_lazy (fun () -> Runtime'.Serialize.serialize (spec ())) in
      fun writer { at; market; slug; visitor; visit; path; query; event; referrer_domain; referrer_path; utm_source; utm_medium; utm_campaign; utm_content; utm_term; browser; browser_version; operating_system; operating_system_version; device; screen; country; region; city; language; params; bot } -> serialize writer at market slug visitor visit path query event referrer_domain referrer_path utm_source utm_medium utm_campaign utm_content utm_term browser browser_version operating_system operating_system_version device screen country region city language params bot

    let to_proto t = let writer = Runtime'.Writer.init () in to_proto' writer t; writer
    let from_proto_exn =
      let constructor at market slug visitor visit path query event referrer_domain referrer_path utm_source utm_medium utm_campaign utm_content utm_term browser browser_version operating_system operating_system_version device screen country region city language params bot = { at; market; slug; visitor; visit; path; query; event; referrer_domain; referrer_path; utm_source; utm_medium; utm_campaign; utm_content; utm_term; browser; browser_version; operating_system; operating_system_version; device; screen; country; region; city; language; params; bot } in
      Runtime'.apply_lazy (fun () -> Runtime'.Deserialize.deserialize (spec ()) constructor)
    let from_proto writer = Runtime'.Result.catch (fun () -> from_proto_exn writer)
    let to_json options =
      let serialize = Runtime'.Serialize_json.serialize ~message_name:(name ()) (spec ()) options in
      fun { at; market; slug; visitor; visit; path; query; event; referrer_domain; referrer_path; utm_source; utm_medium; utm_campaign; utm_content; utm_term; browser; browser_version; operating_system; operating_system_version; device; screen; country; region; city; language; params; bot } -> serialize at market slug visitor visit path query event referrer_domain referrer_path utm_source utm_medium utm_campaign utm_content utm_term browser browser_version operating_system operating_system_version device screen country region city language params bot
    let from_json_exn =
      let constructor at market slug visitor visit path query event referrer_domain referrer_path utm_source utm_medium utm_campaign utm_content utm_term browser browser_version operating_system operating_system_version device screen country region city language params bot = { at; market; slug; visitor; visit; path; query; event; referrer_domain; referrer_path; utm_source; utm_medium; utm_campaign; utm_content; utm_term; browser; browser_version; operating_system; operating_system_version; device; screen; country; region; city; language; params; bot } in
      Runtime'.apply_lazy (fun () -> Runtime'.Deserialize_json.deserialize ~message_name:(name ()) (spec ()) constructor)
    let from_json json = Runtime'.Result.catch (fun () -> from_json_exn json)
  end

  and Visit : sig
    type t = {
      visit:string;
      visitor:string;
      market:string;
      slug:string;
      started:string;
      (**
{%html:
<p>Duration is the difference, carried because it is asked for constantly and
derived identically by everyone.</p>
%}
      *)

      ended:string;
      duration_seconds:int;
      entry_path:string;
      (**
{%html:
<p>The two pages worth naming out of however many they saw.</p>
%}
      *)

      exit_path:string;
      views:int;
      events:int;
      bounce:bool;
      (**
{%html:
<p>One arrival and gone. A convention, not a measurement: defining it
differently makes the number incomparable to everyone else's.</p>
%}
      *)

    }
    val make: ?visit:string -> ?visitor:string -> ?market:string -> ?slug:string -> ?started:string -> ?ended:string -> ?duration_seconds:int -> ?entry_path:string -> ?exit_path:string -> ?views:int -> ?events:int -> ?bounce:bool -> unit -> t
    (** Helper function to generate a message using default values *)

    val to_proto: t -> Runtime'.Writer.t
    (** Serialize the message to binary format *)

    val from_proto: Runtime'.Reader.t -> (t, [> Runtime'.Result.error]) result
    (** Deserialize from binary format *)

    val to_json: Runtime'.Json_options.t -> t -> Runtime'.Json.t
    (** Serialize to Json (compatible with Yojson.Basic.t) *)

    val from_json: Runtime'.Json.t -> (t, [> Runtime'.Result.error]) result
    (** Deserialize from Json (compatible with Yojson.Basic.t) *)

    val name: unit -> string
    (** Fully qualified protobuf name of this message *)

    (**/**)
    type make_t = ?visit:string -> ?visitor:string -> ?market:string -> ?slug:string -> ?started:string -> ?ended:string -> ?duration_seconds:int -> ?entry_path:string -> ?exit_path:string -> ?views:int -> ?events:int -> ?bounce:bool -> unit -> t
    val merge: t -> t -> t
    val to_proto': Runtime'.Writer.t -> t -> unit
    val from_proto_exn: Runtime'.Reader.t -> t
    val from_json_exn: Runtime'.Json.t -> t
    (**/**)
  end = struct
    module This'_ = Visit
    let name () = ".protocol.Visit"
    type t = {
      visit:string;
      visitor:string;
      market:string;
      slug:string;
      started:string;
      ended:string;
      duration_seconds:int;
      entry_path:string;
      exit_path:string;
      views:int;
      events:int;
      bounce:bool;
    }
    type make_t = ?visit:string -> ?visitor:string -> ?market:string -> ?slug:string -> ?started:string -> ?ended:string -> ?duration_seconds:int -> ?entry_path:string -> ?exit_path:string -> ?views:int -> ?events:int -> ?bounce:bool -> unit -> t
    let make ?(visit = {||}) ?(visitor = {||}) ?(market = {||}) ?(slug = {||}) ?(started = {||}) ?(ended = {||}) ?(duration_seconds = 0) ?(entry_path = {||}) ?(exit_path = {||}) ?(views = 0) ?(events = 0) ?(bounce = false) () = { visit; visitor; market; slug; started; ended; duration_seconds; entry_path; exit_path; views; events; bounce }
    let merge =
    let merge_visit = Runtime'.Merge.merge Runtime'.Spec.( basic ((1, "visit", "visit"), string, ({||})) ) in
    let merge_visitor = Runtime'.Merge.merge Runtime'.Spec.( basic ((2, "visitor", "visitor"), string, ({||})) ) in
    let merge_market = Runtime'.Merge.merge Runtime'.Spec.( basic ((3, "market", "market"), string, ({||})) ) in
    let merge_slug = Runtime'.Merge.merge Runtime'.Spec.( basic ((4, "slug", "slug"), string, ({||})) ) in
    let merge_started = Runtime'.Merge.merge Runtime'.Spec.( basic ((5, "started", "started"), string, ({||})) ) in
    let merge_ended = Runtime'.Merge.merge Runtime'.Spec.( basic ((6, "ended", "ended"), string, ({||})) ) in
    let merge_duration_seconds = Runtime'.Merge.merge Runtime'.Spec.( basic ((7, "duration_seconds", "durationSeconds"), uint32_int, (0)) ) in
    let merge_entry_path = Runtime'.Merge.merge Runtime'.Spec.( basic ((8, "entry_path", "entryPath"), string, ({||})) ) in
    let merge_exit_path = Runtime'.Merge.merge Runtime'.Spec.( basic ((9, "exit_path", "exitPath"), string, ({||})) ) in
    let merge_views = Runtime'.Merge.merge Runtime'.Spec.( basic ((10, "views", "views"), uint32_int, (0)) ) in
    let merge_events = Runtime'.Merge.merge Runtime'.Spec.( basic ((11, "events", "events"), uint32_int, (0)) ) in
    let merge_bounce = Runtime'.Merge.merge Runtime'.Spec.( basic ((12, "bounce", "bounce"), bool, (false)) ) in
    fun t1 t2 -> {
    	visit = (merge_visit t1.visit t2.visit);
    	visitor = (merge_visitor t1.visitor t2.visitor);
    	market = (merge_market t1.market t2.market);
    	slug = (merge_slug t1.slug t2.slug);
    	started = (merge_started t1.started t2.started);
    	ended = (merge_ended t1.ended t2.ended);
    	duration_seconds = (merge_duration_seconds t1.duration_seconds t2.duration_seconds);
    	entry_path = (merge_entry_path t1.entry_path t2.entry_path);
    	exit_path = (merge_exit_path t1.exit_path t2.exit_path);
    	views = (merge_views t1.views t2.views);
    	events = (merge_events t1.events t2.events);
    	bounce = (merge_bounce t1.bounce t2.bounce);
     }
    let spec () = Runtime'.Spec.( basic ((1, "visit", "visit"), string, ({||})) ^:: basic ((2, "visitor", "visitor"), string, ({||})) ^:: basic ((3, "market", "market"), string, ({||})) ^:: basic ((4, "slug", "slug"), string, ({||})) ^:: basic ((5, "started", "started"), string, ({||})) ^:: basic ((6, "ended", "ended"), string, ({||})) ^:: basic ((7, "duration_seconds", "durationSeconds"), uint32_int, (0)) ^:: basic ((8, "entry_path", "entryPath"), string, ({||})) ^:: basic ((9, "exit_path", "exitPath"), string, ({||})) ^:: basic ((10, "views", "views"), uint32_int, (0)) ^:: basic ((11, "events", "events"), uint32_int, (0)) ^:: basic ((12, "bounce", "bounce"), bool, (false)) ^:: nil )
    let to_proto' =
      let serialize = Runtime'.apply_lazy (fun () -> Runtime'.Serialize.serialize (spec ())) in
      fun writer { visit; visitor; market; slug; started; ended; duration_seconds; entry_path; exit_path; views; events; bounce } -> serialize writer visit visitor market slug started ended duration_seconds entry_path exit_path views events bounce

    let to_proto t = let writer = Runtime'.Writer.init () in to_proto' writer t; writer
    let from_proto_exn =
      let constructor visit visitor market slug started ended duration_seconds entry_path exit_path views events bounce = { visit; visitor; market; slug; started; ended; duration_seconds; entry_path; exit_path; views; events; bounce } in
      Runtime'.apply_lazy (fun () -> Runtime'.Deserialize.deserialize (spec ()) constructor)
    let from_proto writer = Runtime'.Result.catch (fun () -> from_proto_exn writer)
    let to_json options =
      let serialize = Runtime'.Serialize_json.serialize ~message_name:(name ()) (spec ()) options in
      fun { visit; visitor; market; slug; started; ended; duration_seconds; entry_path; exit_path; views; events; bounce } -> serialize visit visitor market slug started ended duration_seconds entry_path exit_path views events bounce
    let from_json_exn =
      let constructor visit visitor market slug started ended duration_seconds entry_path exit_path views events bounce = { visit; visitor; market; slug; started; ended; duration_seconds; entry_path; exit_path; views; events; bounce } in
      Runtime'.apply_lazy (fun () -> Runtime'.Deserialize_json.deserialize ~message_name:(name ()) (spec ()) constructor)
    let from_json json = Runtime'.Result.catch (fun () -> from_json_exn json)
  end

end

