name: scry

on:
  pull_request:
    paths:
      - 'qntx-plugins/scry/**'
      - 'plugin/grpc/protocol/domain.proto'
      - 'plugin/grpc/protocol/llm.proto'
      - 'plugin/grpc/protocol/proto.cmake'
      - '.github/workflows/scry.yml'
  push:
    branches: [main]
    paths:
      - 'qntx-plugins/scry/**'
      - 'plugin/grpc/protocol/domain.proto'
      - 'plugin/grpc/protocol/llm.proto'
      - 'plugin/grpc/protocol/proto.cmake'

jobs:
  build:
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v5
        with:
          submodules: recursive

      - name: Install Nix
        uses: cachix/install-nix-action@v30
        with:
          extra_nix_config: |
            experimental-features = nix-command flakes

      - name: Setup Cachix
        uses: cachix/cachix-action@v14
        with:
          name: qntx
          authToken: '${{ secrets.CACHIX_AUTH_TOKEN }}'

      - name: Build scry plugin
        run: nix build ./qntx-plugins/scry --show-trace 2>&1 || { nix log $(nix path-info --derivation ./qntx-plugins/scry 2>/dev/null || echo /dev/null) 2>/dev/null | tail -100; exit 1; }

      - name: Verify binary
        run: ./result/bin/qntx-scry-plugin --version

      - name: Push to Cachix
        run: nix build ./qntx-plugins/scry --print-out-paths --no-link | cachix push qntx
