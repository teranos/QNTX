name: Windows Tauri

on:
  pull_request:
    branches: [main]
    paths:
      - 'web/src-tauri/**'
      - 'web/src/**'
      - '.github/workflows/windows-tauri.yml'
  push:
    branches: [main]
    paths:
      - 'web/src-tauri/**'
      - 'web/src/**'
  workflow_dispatch:

jobs:
  tauri-check:
    name: Tauri Check (Windows)
    runs-on: windows-latest
    defaults:
      run:
        shell: bash

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: x86_64-pc-windows-msvc

      - name: Setup Node/Bun
        uses: oven-sh/setup-bun@v2

      - name: Build frontend
        run: make web

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.23'

      - name: Build qntx CLI (sidecar dependency)
        run: make cli

      - name: Prepare Tauri sidecars
        run: |
          mkdir -p web/src-tauri/bin
          cp bin/qntx web/src-tauri/bin/qntx-x86_64-pc-windows-msvc.exe

      - name: Cache Rust dependencies
        uses: swatinem/rust-cache@v2
        with:
          workspaces: web/src-tauri

      - name: Check formatting
        run: cd web/src-tauri && cargo fmt --check

      - name: Clippy
        run: cd web/src-tauri && cargo clippy --all-targets --all-features
        env:
          RUSTFLAGS: "-Dwarnings"

      - name: Check (x64)
        run: cd web/src-tauri && cargo check --target x86_64-pc-windows-msvc
