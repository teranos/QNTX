name: Branch Latest

# Rolling pre-release per branch. On push to any branch, publishes/updates
# a pre-release tagged `branch-<name>-latest` with Linux tarballs.
#
# Deployments (e.g. q.sbvh.nl) that need a stable URL for the current tip of a
# branch can fetch:
#   https://github.com/teranos/QNTX/releases/download/branch-<name>-latest/qntx-<name>-linux-<arch>.tar.gz
#
# Same tarball + sha256 shape as tag releases (release.yml).
# Linux-only (deployment target). Add other platforms if a use case appears.

on:
  push:
    branches:
      - '**'

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    strategy:
      matrix:
        arch: [amd64, arm64]

    steps:
      - name: Checkout repository
        uses: actions/checkout@v5

      - name: Compute branch name
        id: branch
        run: |
          # Sanitize the ref for use in a git tag: replace '/' with '-'.
          BRANCH_NAME="${GITHUB_REF#refs/heads/}"
          SAFE_NAME="${BRANCH_NAME//\//-}"
          echo "name=$SAFE_NAME" >> $GITHUB_OUTPUT
          echo "tag=branch-${SAFE_NAME}-latest" >> $GITHUB_OUTPUT
          echo "Branch: $BRANCH_NAME  Tag: branch-${SAFE_NAME}-latest"

      - name: Install Nix
        uses: cachix/install-nix-action@v26
        with:
          extra_nix_config: |
            experimental-features = nix-command flakes

      - name: Setup Cachix
        uses: cachix/cachix-action@v14
        with:
          name: qntx
          authToken: '${{ secrets.CACHIX_AUTH_TOKEN }}'

      - name: Build QNTX binary
        run: |
          echo "Building QNTX for Linux ${{ matrix.arch }}..."
          nix build .#qntx --print-build-logs

      - name: Cache binary to Cachix
        run: nix build .#qntx --print-out-paths --no-link | cachix push qntx

      - name: Package binary
        run: |
          mkdir -p release
          cp result/bin/qntx release/
          cd release
          tar -czf qntx-${{ steps.branch.outputs.name }}-linux-${{ matrix.arch }}.tar.gz qntx
          sha256sum qntx-${{ steps.branch.outputs.name }}-linux-${{ matrix.arch }}.tar.gz \
            > qntx-${{ steps.branch.outputs.name }}-linux-${{ matrix.arch }}.tar.gz.sha256

      - name: Publish rolling pre-release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ steps.branch.outputs.tag }}
          name: "Branch ${{ steps.branch.outputs.name }} (rolling)"
          prerelease: true
          make_latest: false
          # Delete + recreate the tag on each push so the URL stays stable.
          target_commitish: ${{ github.sha }}
          files: |
            release/qntx-${{ steps.branch.outputs.name }}-linux-${{ matrix.arch }}.tar.gz
            release/qntx-${{ steps.branch.outputs.name }}-linux-${{ matrix.arch }}.tar.gz.sha256
          body: |
            Rolling pre-release for the tip of `${{ steps.branch.outputs.name }}`.

            **URL scheme (stable across pushes):**
            ```
            https://github.com/teranos/QNTX/releases/download/${{ steps.branch.outputs.tag }}/qntx-${{ steps.branch.outputs.name }}-linux-<arch>.tar.gz
            ```

            **Commit:** `${{ github.sha }}`
            **Build tags:** `rustsqlite,qntxwasm` (from flake.nix).

            Not a stable release — for deployment tracking only. Use a `v*` tag for production.
