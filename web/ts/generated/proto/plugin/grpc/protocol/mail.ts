// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.2
//   protoc               v6.32.1
// source: plugin/grpc/protocol/mail.proto

/* eslint-disable */

export const protobufPackage = "protocol";

/**
 * MailTemplate is what a mail is filled from. Each part is a Go template over
 * the values a Send names: subject and text with text/template, html with
 * html/template. A value a template names and the Send does not is a refusal,
 * never an empty space in a mail that went out.
 */
export interface MailTemplate {
  subject: string;
  html: string;
  text: string;
}

export interface SetMailTemplateRequest {
  auth_token: string;
  /** The plugin setting it. A plugin's templates are its own. */
  source: string;
  /** The template's name within the plugin. */
  name: string;
  template: MailTemplate | undefined;
}

export interface SetMailTemplateResponse {
  success: boolean;
  error: string;
  /** The attestation the template is kept as. */
  attestation_id: string;
}

export interface SendMailRequest {
  auth_token: string;
  /** The plugin sending. */
  source: string;
  /** The User it goes to. Their primary address is the one used. */
  user_id: string;
  /** A name the plugin set. Empty is QNTX's neutral template. */
  template: string;
  /** What the template is filled with. */
  values: { [key: string]: string };
  /** Images the html shows by cid:<content_id>. */
  inline: MailImage[];
}

export interface SendMailRequest_ValuesEntry {
  key: string;
  value: string;
}

/**
 * MailImage is an image a mail carries inline. Only image/png is accepted, and
 * all of a mail's images together are held under a size cap.
 */
export interface MailImage {
  /** The html shows it by cid:<content_id>. Unique within a mail. */
  content_id: string;
  /** image/png. */
  content_type: string;
  /** Empty is <content_id>.png. */
  file_name: string;
  data: Uint8Array;
}

export interface SendMailResponse {
  success: boolean;
  error: string;
  /** The id the transport gave the mail. */
  message_id: string;
  /** The attestation of the mail. */
  attestation_id: string;
}
